/**
 * MUSE Extension
 *
 * Modular Unstructured Scoping Engine
 *
 * Takes raw ideas through type-aware guided exploration and produces
 * structured plans ready for CODA's FORGE process.
 *
 * Commands:
 *   /muse          — start ideation (type-first guided exploration)
 *   /muse status   — show all ideas and their state
 *   /muse graduate — promote an idea to a CODA project
 *
 * Integration with CODA:
 *   - MUSE produces a PLANNING.md with structured scope
 *   - `/coda forge` can read MUSE's output to bootstrap the design phase
 *   - Many MUSE ideas never make it to CODA — that's the point
 *
 * Phase 2 — stubbed for now.
 */
