/**
 * LENS Extension
 *
 * Layered Epistemic aNalysis System
 *
 * Multi-domain codebase audit with full epistemic rigor:
 *   - 14 audit domains (architecture, security, data, correctness, etc.)
 *   - Multi-agent personas (architect, security engineer, data engineer, etc.)
 *   - Full 7-layer epistemic schema (observation → evidence → interpretation →
 *     assumption → risk → severity → recommendation)
 *   - Devil's Advocate adversarial review
 *   - Pattern corpus accumulation
 *
 * Commands:
 *   /lens audit     — full multi-domain audit
 *   /lens transform — convert findings into CODA-compatible remediation plan
 *   /lens status    — show audit progress
 *
 * Integration with CODA:
 *   - Brownfield FORGE can escalate to LENS for deep analysis
 *   - `/coda audit` can invoke LENS if installed
 *   - LENS findings import into CODA's gap analysis
 *   - LENS transform produces CODA milestones with risk metadata
 *
 * Shares module prompts with CODA (from /modules/) but runs them at full depth
 * with the complete epistemic schema, not CODA's lightweight assumption-only variant.
 *
 * Phase 3 — stubbed for now.
 */
