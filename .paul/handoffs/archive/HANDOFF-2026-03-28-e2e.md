# PAUL Handoff

**Date:** 2026-03-28
**Status:** paused

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** coda-ecosystem — A suite of Pi extensions for disciplined, agent-assisted software development.
**Core value:** Enabling developers to build durable, maintainable software through disciplined agent-assisted workflows.

---

## Current State

**Version:** 0.1.0
**Phase:** 8 of 8 — E2E Test (FINAL PHASE)
**Plan:** 08-01 created, awaiting APPLY approval

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○     [Plan created, awaiting approval]
```

## Git State

| Field | Value |
|-------|-------|
| Branch | feature/07-pi-integration |
| Base | main |
| PR | https://github.com/coctostan/coda/pull/5 (OPEN) |
| CI | N/A |
| Behind base | Up to date |

---

## What Was Done

- **Phases 1-7 complete** — Full CODA stack built:
  - M1: Data Layer (29 tests) — mdbase records, YAML frontmatter, sections, directory listing
  - M2: State Engine (38 tests) — phase transitions, gates, atomic JSON persist
  - M3: Tool Layer (51 tests) — 7 coda_* tools + write-gate
  - M4: Two Modules (11 tests) — Todd TDD + Walt quality prompts with TS loaders
  - M5: FORGE (20 tests) — greenfield scaffold + ref docs + milestone creation
  - M6: Workflow Engine (24 tests) — context builder, phase runner, build loop
  - M7: Pi Integration (14 tests) — commands, tools, hooks, extension entry point
- **187 tests passing**, TypeScript strict, zero external deps
- **5 PRs open** on GitHub (coctostan/coda): #1 merged, #2-#5 open
- **Smoke test performed** — opened CMUX pane, tried loading CODA as Pi extension
- **Critical finding:** M7 Pi layer used a custom `PiAPI` interface instead of the real `ExtensionAPI` from `@mariozechner/pi-coding-agent`. Extension failed to load.
- **Plan 08-01 rewritten** to include Task 1: rewrite Pi layer against real `ExtensionAPI`

---

## What's In Progress

- **Plan 08-01 is ready and awaiting APPLY approval**
  - Plan file: `.paul/phases/08-e2e-test/08-01-PLAN.md`
  - 4 tasks:
    1. Rewrite Pi layer against real ExtensionAPI (fix module format, use TypeBox, proper event signatures)
    2. Write Live CMUX Pane Test Script v1
    3. Execute the script (checkpoint — live dogfood)
    4. Report findings
  - autonomous: false (Task 3 is a checkpoint)

---

## What's Next

**Immediate:** Approve and run APPLY for Plan 08-01

**Task 1 specifics (Pi layer rewrite):**
- Add `"type": "module"` to packages/coda/package.json
- Replace custom `PiAPI` with `ExtensionAPI` from `@mariozechner/pi-coding-agent`
- Use `pi.registerTool({ name, label, parameters: Type.Object(...), execute })` not `(name, schema, handler)`
- Use `pi.registerCommand(name, { handler: async (args, ctx) => {} })` where args is string not string[]
- Use `pi.on("before_agent_start", async (event, ctx) => {})` with proper event types
- Use `@sinclair/typebox` `Type.Object()` for parameters

**After Task 1:** Tasks 2-4 are the live E2E dogfood test via CMUX pane

**Reference files for the rewrite:**
- `/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/docs/extensions.md` — canonical Pi extension docs
- `/tmp/pi-github-repos/ayagmar/pi-extension-template/src/index.ts` — working template example
- `/tmp/pi-github-repos/ayagmar/pi-extension-template/package.json` — package.json shape with `"type": "module"`

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (7/8 complete) |
| `.paul/PROJECT.md` | Project brief |
| `.paul/phases/08-e2e-test/08-01-PLAN.md` | Current plan (ready for APPLY) |
| `packages/coda/src/pi/` | Pi layer (needs rewrite in Task 1) |
| `docs/v0.1/07-pi-integration.md` | M7 spec |

---

## Key Decisions This Session

| Decision | Rationale |
|----------|-----------|
| Prompts as .md data loaded by TS | AGENTS.md convention |
| PiAPI as interface (original M7) | Decoupling — but turned out wrong API |
| Rewrite M7 in Phase 8 Task 1 | Smoke test proved custom PiAPI doesn't match real ExtensionAPI |
| E2E as live CMUX pane dogfood | Real integration test, not mocked — verifies actual extension loading |
| Model: openai-codex/gpt-5.4-mini | For the E2E test Pi session |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. You're on branch `feature/07-pi-integration` with plan 08-01 ready
3. Run `/paul:resume` — it will route to APPLY approval for Plan 08-01
4. Task 1 rewrites Pi layer, then Tasks 2-4 are the live E2E dogfood
5. Reference the Pi extension docs and template (paths in "What's Next" above)

---

*Handoff created: 2026-03-28*
