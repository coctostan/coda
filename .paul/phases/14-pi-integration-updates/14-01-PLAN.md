---
phase: 14-pi-integration-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coda/src/pi/hooks.ts
  - packages/coda/src/pi/commands.ts
  - packages/coda/src/pi/types.ts
  - packages/coda/src/pi/__tests__/hooks.test.ts
  - packages/coda/src/pi/__tests__/commands.test.ts
  - packages/coda/src/tools/coda-status.ts
  - packages/coda/src/tools/types.ts
  - packages/coda/src/tools/__tests__/coda-status.test.ts
  - packages/coda/src/workflow/context-builder.ts
  - packages/coda/src/workflow/phase-runner.ts
  - packages/coda/src/workflow/types.ts
  - packages/coda/src/workflow/__tests__/context-builder.test.ts
  - packages/coda/src/workflow/__tests__/phase-runner.test.ts
autonomous: true
---

<objective>
## Goal
Finish the v0.2 Pi integration slice so `before_agent_start` and `/coda` command handling expose the correct submode-aware runtime context for review/revise/verify/correct flows without widening scope beyond the existing lifecycle model.

## Purpose
Phases 10–13 established the review, revise, verify, correct, human-review, exhaustion, rewind, and kill mechanics. Phase 14 makes those mechanics usable through the Pi extension layer so the live extension presents the right context and operator guidance during autonomous-loop work.

## Output
- Submode-aware `before_agent_start` context injection for review/revise/verify/correct
- Pi command/status behavior that reflects submode-specific runtime state instead of generic phase-only messaging
- Regression tests covering hooks, commands, workflow context assembly, and status output for the integrated v0.2 paths
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
ROADMAP.md

## Prior Work (only if genuinely needed)
.paul/phases/13-exhaustion-handling-rewind-kill-controls/13-01-SUMMARY.md

## Source-of-Truth / Reference Docs
AGENTS.md
pals.json
docs/coda-spec-v7.md
docs/v0.1/07-pi-integration.md

## Source Files
packages/coda/src/pi/hooks.ts
packages/coda/src/pi/commands.ts
packages/coda/src/pi/types.ts
packages/coda/src/tools/coda-status.ts
packages/coda/src/tools/types.ts
packages/coda/src/workflow/context-builder.ts
packages/coda/src/workflow/phase-runner.ts
packages/coda/src/workflow/types.ts
packages/coda/src/pi/__tests__/hooks.test.ts
packages/coda/src/pi/__tests__/commands.test.ts
packages/coda/src/tools/__tests__/coda-status.test.ts
packages/coda/src/workflow/__tests__/context-builder.test.ts
packages/coda/src/workflow/__tests__/phase-runner.test.ts
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: arch(75) → feature-based monorepo structure detected under `packages/*`; scoped files stay inside `packages/coda/src/{pi,tools,workflow}` with no obvious layer-placement concerns, though `hooks.test.ts` (455 lines), `commands.test.ts` (382), `context-builder.ts` (343), and `coda-status.test.ts` (318) are moderate-size hotspots; todd(100) → Bun test infrastructure detected (`bun test` in root and package manifests); iris(150) → 1 anti-pattern marker in scoped tests (`HACKED` string inside a write-protection assertion), 0 production anti-pattern markers; gabe(175) → no API-route files in scope; luke(185) → no UI files in scope; aria(190) → no UI files in scope; dana(195) → no data/schema files in scope; dave(200) → no repo CI config detected outside ignored dependency trees; docs(200) → implementation docs exist (`docs/coda-spec-v7.md`, `docs/v0.1/07-pi-integration.md`), but Phase 14 may need follow-up doc alignment if runtime behavior diverges; omar(205) → observability review not material for the scoped Pi/workflow files; reed(210) → resilience-specific concerns not material for the scoped Pi/workflow files; vera(215) → no privacy-relevant files in scope; pete(225) → no heavy-import or obvious performance-sensitive files in scope; ruby(250) → current scoped files are below the 500-line critical threshold but several moderate-size files should avoid opportunistic expansion.

[dispatch] pre-plan enforcement: dean(50) → `bun audit --json` returned `{}` (no dependency findings); seth(80) → no hardcoded secrets detected in scoped files.
</module_dispatch>

<acceptance_criteria>
## AC-1: `before_agent_start` injects phase-correct submode context for autonomous loops
```gherkin
Given the focused issue is in review/revise or verify/correct submodes
When the Pi extension receives `before_agent_start`
Then it returns the system prompt and context assembled for that exact submode
And the hook payload includes enough runtime metadata for Pi-facing consumers to identify the active phase, submode, loop iteration, and task context
```

## AC-2: `/coda` command handling reflects submode-specific runtime state
```gherkin
Given the focused issue is in a submode-aware review or verify flow
When `/coda status`, `/coda advance`, or `/coda build` is invoked through the Pi extension
Then the user-visible output reflects the active submode and valid next action for that state
And command routing does not fall back to generic phase-only messaging where the submode materially changes behavior
```

## AC-3: Phase 14 stays scoped to Pi/runtime integration rather than new lifecycle mechanics
```gherkin
Given the Phase 14 implementation is complete
When targeted integration tests, the full Bun suite, and TypeScript typecheck are run
Then they pass without regressions
And the changes remain limited to Pi hooks, command/status integration, and context assembly for existing review/revise/verify/correct flows
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add failing integration tests for submode-aware hooks, commands, and status output</name>
  <files>packages/coda/src/pi/__tests__/hooks.test.ts, packages/coda/src/pi/__tests__/commands.test.ts, packages/coda/src/tools/__tests__/coda-status.test.ts, packages/coda/src/workflow/__tests__/context-builder.test.ts, packages/coda/src/workflow/__tests__/phase-runner.test.ts</files>
  <action>
    Add test-first coverage for the Phase 14 integration slice before changing production code:
    - Extend `hooks.test.ts` to assert that `before_agent_start` returns review/revise/verify/correct-specific context and Pi-facing metadata (phase, submode, loop iteration, and task details where relevant).
    - Extend `commands.test.ts` to cover submode-aware command behavior where Pi notifications should differ from generic phase-only output.
    - Extend `coda-status.test.ts` so status results expose the fields and guidance needed by the Pi command layer for these submodes.
    - Extend `context-builder.test.ts` and `phase-runner.test.ts` only as needed to lock down the exact context sections the hook must surface for revise and correct paths.
    Avoid implementation-only assertions; verify observable hook payloads, command notifications, and assembled context content.
  </action>
  <verify>bun test packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/workflow/__tests__/context-builder.test.ts packages/coda/src/workflow/__tests__/phase-runner.test.ts</verify>
  <done>AC-1 and AC-2 are represented by failing or incomplete tests before production edits begin.</done>
</task>

<task type="auto">
  <name>Task 2: Implement submode-aware Pi hook, status, and command integration</name>
  <files>packages/coda/src/pi/hooks.ts, packages/coda/src/pi/commands.ts, packages/coda/src/pi/types.ts, packages/coda/src/tools/coda-status.ts, packages/coda/src/tools/types.ts, packages/coda/src/workflow/context-builder.ts, packages/coda/src/workflow/phase-runner.ts, packages/coda/src/workflow/types.ts</files>
  <action>
    Implement the Phase 14 runtime integration using the existing CODA primitives:
    - Extend workflow/context assembly only where the Pi hook needs explicit submode-aware sections or metadata that are not already exposed cleanly.
    - Update `hooks.ts` and related Pi types so `before_agent_start` forwards the exact review/revise/verify/correct context plus durable runtime details needed by the extension surface.
    - Update `coda-status.ts`, tool types, and `commands.ts` so `/coda` output and routing can distinguish active submodes when the valid operator action differs from the generic phase default.
    - Reuse existing review/verify/human-review/exhaustion behavior; do not introduce new lifecycle states, new persistence layers, or alternate control paths.
    Avoid broad refactors in the build loop, review runner, verify runner, or docs unless a narrow integration fix requires touching them.
  </action>
  <verify>bun test packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/workflow/__tests__/context-builder.test.ts packages/coda/src/workflow/__tests__/phase-runner.test.ts</verify>
  <done>AC-1 and AC-2 are satisfied by shipped Pi hook, command, and status behavior for review/revise/verify/correct flows.</done>
</task>

<task type="auto">
  <name>Task 3: Run full verification and keep the slice isolated to Phase 14 integration work</name>
  <files>packages/coda/src/pi/hooks.ts, packages/coda/src/pi/commands.ts, packages/coda/src/pi/types.ts, packages/coda/src/tools/coda-status.ts, packages/coda/src/tools/types.ts, packages/coda/src/workflow/context-builder.ts, packages/coda/src/workflow/phase-runner.ts, packages/coda/src/workflow/types.ts, packages/coda/src/pi/__tests__/hooks.test.ts, packages/coda/src/pi/__tests__/commands.test.ts, packages/coda/src/tools/__tests__/coda-status.test.ts, packages/coda/src/workflow/__tests__/context-builder.test.ts, packages/coda/src/workflow/__tests__/phase-runner.test.ts</files>
  <action>
    Run the targeted and repo-wide verification gates, then tighten only the code required to keep the integration slice coherent:
    - Run the targeted hook/command/context tests first, then `bun test`, then `npx tsc --noEmit`.
    - Fix any regression or type drift introduced by the new Pi/runtime integration fields.
    - Confirm the final change remains limited to Pi hook payloads, command/status behavior, and submode-aware context injection for existing lifecycle mechanics.
    Avoid opportunistic redesign of the lifecycle model, Phase 15 E2E scenarios, or the broader docs cleanup backlog once the Phase 14 gates are green.
  </action>
  <verify>bun test packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/workflow/__tests__/context-builder.test.ts packages/coda/src/workflow/__tests__/phase-runner.test.ts && bun test && npx tsc --noEmit</verify>
  <done>AC-3 is satisfied and the Phase 14 work stays isolated to Pi/runtime integration for existing submode flows.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Phase 15 E2E scenario planning or new end-to-end fixtures beyond narrow regression coverage strictly needed for Phase 14
- Core lifecycle semantics in `@coda/core` (phase order, gates, submode model, persistence contract)
- Phase 13 rewind/kill mechanics except where Pi integration must surface existing behavior correctly
- README / CHANGELOG / broad docs cleanup beyond minimal implementation-alignment edits that become strictly necessary

## SCOPE LIMITS
- This plan implements only Pi hook, command, status, and context-injection updates for existing review/revise/verify/correct flows.
- Reuse existing CODA workflow/tool primitives rather than inventing a separate Pi-only orchestration layer.
- Do not add new dependencies.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `bun test packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/workflow/__tests__/context-builder.test.ts packages/coda/src/workflow/__tests__/phase-runner.test.ts`
- [ ] `bun test`
- [ ] `npx tsc --noEmit`
- [ ] Confirm `before_agent_start` exposes the correct review/revise/verify/correct context and runtime metadata
- [ ] Confirm `/coda` command output reflects the active submode where behavior differs from the generic phase default
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- All three tasks completed in order with test-first evidence
- Pi hook context is phase-correct for review/revise/verify/correct runtime states
- `/coda` command and status output reflect submode-specific operator guidance where needed
- Full test suite and typecheck remain green
- Phase 14 changes stay inside Pi integration and context assembly without widening lifecycle scope
</success_criteria>

<output>
After completion, create `.paul/phases/14-pi-integration-updates/14-01-SUMMARY.md`
</output>
