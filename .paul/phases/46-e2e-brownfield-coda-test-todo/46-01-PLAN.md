---
phase: 46-e2e-brownfield-coda-test-todo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coda/src/forge/__tests__/brownfield-e2e.test.ts
autonomous: true
---

<objective>
## Goal
E2E integration test that validates the entire brownfield FORGE pipeline: detection → scaffold → SCAN → SYNTHESIZE → GAP ANALYSIS → VALIDATE → ORIENT, plus context features (topic retrieval, ceremony, budgets).

## Purpose
Validates that all v0.7 components work together end-to-end. Creates a realistic mock brownfield project and exercises every stage of the pipeline programmatically.

## Output
- New `brownfield-e2e.test.ts` with comprehensive integration tests
- Full suite passes with no regressions
</objective>

<context>
## Project Context
.paul/PROJECT.md

## Spec
docs/v0.4/E2E-TEST-SCRIPT-v0.4.md

## Source Files
All brownfield + context modules from Phases 36-45
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH(p75) → New test file, no source changes | TODD(p100) → E2E validation phase
[dispatch] pre-plan enforcement: DEAN(p50) → skip
</module_dispatch>

<acceptance_criteria>

## AC-1: Full brownfield pipeline works
```gherkin
Given a mock project with package.json, src/, README.md
When the brownfield pipeline is run end-to-end
Then detection, scaffold, scan, synthesize, gap, validate, orient all produce correct output
```

## AC-2: Context features work
```gherkin
Given the topic retrieval, ceremony, and budget modules
When exercised with brownfield context
Then topic matching filters sections, ceremony returns correct rules, budgets assemble within limits
```

## AC-3: Evidence round-trips
```gherkin
Given evidence written during SCAN simulation
When read back during SYNTHESIZE and GAP contexts
Then all evidence content is preserved
```

## AC-4: All artifacts producible
```gherkin
Given the full pipeline
When gap analysis and milestone plan are written
Then both artifacts are readable with correct frontmatter
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create brownfield E2E integration test</name>
  <files>packages/coda/src/forge/__tests__/brownfield-e2e.test.ts</files>
  <action>
    Create new E2E test file that exercises the full brownfield pipeline.

    **Setup:** Create a realistic mock project in a temp dir:
    - `package.json` with name/version/scripts/dependencies
    - `src/index.ts` (small file)
    - `README.md` with project description
    - `src/store.ts` (small file)

    **Test 1: Full pipeline integration**
    - detectBackdrop → brownfield
    - scaffoldCoda → .coda/ created
    - assembleScanContext → universalTargets include README.md + package.json, sourceDir = 'src'
    - Write evidence files (simulating agent output): EVIDENCE-universal.md, EVIDENCE-architecture.md, EVIDENCE-security.md
    - assembleSynthesizeContext → 3 evidence records, 4 ref docs
    - assembleGapAnalysisContext → 3 evidence records, 5 domains
    - writeGapAnalysis → file exists
    - assembleValidateContext → gap + evidence summary + 4 questions
    - assembleOrientContext → gap + evidence summary + 4 questions
    - writeMilestonePlan → file exists
    - readMilestonePlan → round-trips

    **Test 2: Context features integration**
    - getSectionsByTopics with evidence body → returns matching sections
    - getSectionHeadings on evidence → returns heading list
    - getCeremonyRules('bugfix') → tddEnabled=true, verifyMaxIterations=2
    - getCeremonyRules('chore') → tddEnabled=false, modulesEnabled=false
    - assembleWithinBudget with mock sections → respects priority ordering
    - estimateTokens on sample text → reasonable estimate

    **Test 3: Module init-scan hooks verify**
    - createRegistry({}, promptsDir).getModulesForHook('init-scan') → 5 modules
    - Architecture first (priority 50), knowledge last (200)

    **Test 4: Evidence persistence round-trip**
    - Write 3 evidence files → readAllEvidence returns 3 sorted
    - readEvidence by name → correct content
    - writeGapAnalysis → readGapAnalysis → content preserved
    - writeMilestonePlan → readMilestonePlan → content preserved

    Use mkdtempSync, beforeEach/afterEach cleanup. Import from both forge and core packages.
  </action>
  <verify>bun test packages/coda/src/forge/__tests__/brownfield-e2e.test.ts</verify>
  <done>All E2E tests pass. AC-1 through AC-4 verified. Full suite green.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any existing source files
- Any existing test files

## SCOPE LIMITS
- E2E test file only — no source changes
- Programmatic validation (no live CMUX session)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `bun test packages/coda/src/forge/__tests__/brownfield-e2e.test.ts` — all tests pass
- [ ] `bun test` — full suite passes, no regressions
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All E2E tests pass
- Full suite green (no regressions)
- ≥ 4 E2E tests covering full pipeline + context features + hooks + persistence
</success_criteria>

<output>
After completion, create `.paul/phases/46-e2e-brownfield-coda-test-todo/46-01-SUMMARY.md`
</output>
