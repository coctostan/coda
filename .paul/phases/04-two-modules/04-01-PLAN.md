---
phase: 04-two-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/prompts/todd.md
  - modules/prompts/walt.md
  - packages/coda/src/modules/todd.ts
  - packages/coda/src/modules/walt.ts
  - packages/coda/src/modules/index.ts
  - packages/coda/src/modules/__tests__/modules.test.ts
autonomous: true
---

<objective>
## Goal
Create the Todd (TDD enforcement) and Walt (quality baseline) module prompts as markdown data files, with TypeScript loaders that read and export them. These are the first two CODA modules — pure prompt injection, no registry or dispatcher.

## Purpose
Modules are the behavioral layer that shapes how the LLM interacts with the development lifecycle. Todd enforces RED-GREEN-REFACTOR discipline. Walt catches regressions after each task. In v0.1, they're simple prompts injected by the workflow engine (M6) at the right moments.

## Output
- `modules/prompts/todd.md` and `modules/prompts/walt.md` — prompt data files
- `packages/coda/src/modules/` — TypeScript loaders + barrel export
- Tests verifying prompt loading and hook-point routing
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md

## Source Files
docs/v0.1/04-modules.md
packages/coda/src/index.ts
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → 118 tests baseline | IRIS(150) → clean | DOCS(200) → Spec at docs/v0.1/04-modules.md | RUBY(250) → No debt
[dispatch] pre-plan enforcement: DEAN(50) → clean | SETH(80) → clean
</module_dispatch>

<acceptance_criteria>

## AC-1: Todd Prompt Exists as Markdown Data
```gherkin
Given the file modules/prompts/todd.md
When it is read
Then it contains TDD enforcement instructions including RED-GREEN cycle, write-gate explanation, and anti-patterns
```

## AC-2: Walt Prompt Exists as Markdown Data
```gherkin
Given the file modules/prompts/walt.md
When it is read
Then it contains post-task quality check instructions including regression rules and baseline comparison
```

## AC-3: TypeScript Loaders Export Prompts
```gherkin
Given the todd.ts and walt.ts loader modules
When imported
Then they export the prompt content as strings loaded from the markdown files
```

## AC-4: Hook-Point Routing
```gherkin
Given a getModulePrompts function
When called with "pre-build"
Then it returns an array containing Todd's prompt
When called with "post-task"
Then it returns an array containing Walt's prompt
When called with "pre-plan"
Then it returns an empty array
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create module prompt markdown files</name>
  <files>modules/prompts/todd.md, modules/prompts/walt.md</files>
  <action>
    1. Create `modules/prompts/todd.md` with this content:

       ```
       ## TDD Enforcement

       You MUST follow the RED-GREEN cycle for this task:

       1. **RED:** Write a failing test FIRST that covers the task's behavior
       2. Run `coda_run_tests({ mode: "tdd" })` to confirm the test fails
       3. Verify the failure is for the RIGHT reason (missing implementation, not syntax error)
       4. **GREEN:** Write the MINIMUM production code to make the test pass
       5. Run `coda_run_tests({ mode: "tdd" })` to confirm it passes
       6. Do NOT write more production code until you write the next failing test

       The write-gate enforces this mechanically:
       - Production file writes are BLOCKED until you have a failing test
       - After tests pass, production writes are re-blocked
       - You must write the next test before writing more production code

       ### Anti-Patterns to Avoid
       - **Never change tests to match broken implementation** — tests define the contract
       - **Never skip RED verification** — a passing test you expected to fail means something is wrong
       - **Never over-engineer in GREEN** — simplest passing code only; refactor after

       If a task has no testable behavior (e.g., config changes), note this
       explicitly and proceed without TDD.
       ```

    2. Create `modules/prompts/walt.md` with this content:

       ```
       ## Post-Task Quality Check

       After completing this task, verify quality:

       1. Run the full test suite: `coda_run_tests({ mode: "suite" })`
       2. Compare against baseline — only NEW failures are regressions
          (pre-existing failures are not your fault, but don't add more)
       3. If tests fail, fix regressions before proceeding to the next task
       4. Run any verification commands configured in coda.json
          (typically: lint, typecheck)
       5. If verification fails, fix issues before proceeding

       ### Regression Rules
       - Tests that passed before but fail now = REGRESSION — fix required
       - Tests that already failed before = KNOWN FAILURE — not blocking
       - New tests that fail = expected during TDD RED phase, not a regression

       Do NOT advance to the next task until all checks pass.
       ```

    Avoid: Putting prompts in TypeScript — they are data files per AGENTS.md convention.
  </action>
  <verify>cat modules/prompts/todd.md && cat modules/prompts/walt.md</verify>
  <done>AC-1, AC-2 satisfied: Prompt data files exist with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript loaders, hook routing, and tests</name>
  <files>packages/coda/src/modules/todd.ts, packages/coda/src/modules/walt.ts, packages/coda/src/modules/index.ts, packages/coda/src/modules/__tests__/modules.test.ts</files>
  <action>
    1. Create `packages/coda/src/modules/__tests__/modules.test.ts` FIRST (TDD):
       - Test: TODD_PROMPT is a non-empty string containing "RED-GREEN"
       - Test: TODD_PROMPT contains "write-gate" and "Anti-Patterns"
       - Test: WALT_PROMPT is a non-empty string containing "Quality Check"
       - Test: WALT_PROMPT contains "Regression Rules"
       - Test: getModulePrompts("pre-build") returns array containing Todd's prompt
       - Test: getModulePrompts("post-task") returns array containing Walt's prompt
       - Test: getModulePrompts("post-build") returns array containing Walt's prompt
       - Test: getModulePrompts("pre-plan") returns empty array

    2. Create `packages/coda/src/modules/todd.ts`:
       - Read `modules/prompts/todd.md` at module load time
       - Export `TODD_PROMPT: string` — the full prompt content
       - Export `TODD_HOOK_POINTS: string[]` — `["pre-build"]`
       - Use `readFileSync` with path resolved relative to the repo root
       - For path resolution: use `import.meta.dir` or `__dirname` to find the repo root
         by walking up from `packages/coda/src/modules/` to the repo root (4 levels up)
       - JSDoc on exports

    3. Create `packages/coda/src/modules/walt.ts`:
       - Read `modules/prompts/walt.md` at module load time
       - Export `WALT_PROMPT: string` — the full prompt content
       - Export `WALT_HOOK_POINTS: string[]` — `["post-task", "post-build"]`
       - Same path resolution approach as todd.ts
       - JSDoc on exports

    4. Create `packages/coda/src/modules/index.ts`:
       - Re-export TODD_PROMPT, TODD_HOOK_POINTS from `./todd`
       - Re-export WALT_PROMPT, WALT_HOOK_POINTS from `./walt`
       - Implement `getModulePrompts(hookPoint: string): string[]`
         - Check TODD_HOOK_POINTS — if hookPoint matches, include TODD_PROMPT
         - Check WALT_HOOK_POINTS — if hookPoint matches, include WALT_PROMPT
         - Return empty array if no modules match
       - JSDoc on getModulePrompts

    5. Update `packages/coda/src/index.ts`:
       - Add `export * from './modules';` after the tools export
       - Keep forge/workflow/pi commented out

    Avoid: Hardcoding prompt text in TypeScript — load from the .md files. Do NOT add `any` types.
  </action>
  <verify>cd packages/coda && bun test src/modules/__tests__/modules.test.ts && cd ../core && bun test</verify>
  <done>AC-3, AC-4 satisfied: Loaders export prompt content, hook routing returns correct prompts</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- packages/core/* (Phases 1-2 — stable)
- packages/coda/src/tools/* (Phase 3 — stable)
- packages/muse/*, packages/lens/*, packages/helm/* (stubbed)
- docs/* (specs are read-only reference)

## SCOPE LIMITS
- Two modules only (Todd + Walt) — no registry, dispatcher, or eval engine
- Prompts are static markdown files — no templating or variable substitution in v0.1
- getModulePrompts is a simple lookup — no priority ordering or hook metadata
- No module infrastructure (that's post-v0.1)
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `modules/prompts/todd.md` exists with TDD enforcement content
- [ ] `modules/prompts/walt.md` exists with quality check content
- [ ] `cd packages/coda && bun test` — all tests pass (tools + modules)
- [ ] `cd packages/core && bun test` — all 67 core tests pass (no regressions)
- [ ] `npx tsc --noEmit` — no TypeScript errors
- [ ] TODD_PROMPT loaded from markdown file, not hardcoded
- [ ] WALT_PROMPT loaded from markdown file, not hardcoded
- [ ] getModulePrompts routes correctly for all hook points
- [ ] No `any` types
- [ ] All public functions have JSDoc comments
</verification>

<success_criteria>
- All 2 tasks completed
- All test files pass via `bun test`
- TypeScript compiles with zero errors
- 4 acceptance criteria met
- Zero regressions in core and tool tests
</success_criteria>

<output>
After completion, create `.paul/phases/04-two-modules/04-01-SUMMARY.md`
</output>
