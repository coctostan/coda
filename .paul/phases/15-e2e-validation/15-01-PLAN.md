---
phase: 15-e2e-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/coda/src/workflow/__tests__/autonomous-loops-e2e.test.ts
  - packages/coda/src/workflow/__tests__/review-runner.test.ts
  - packages/coda/src/workflow/__tests__/verify-runner.test.ts
  - packages/coda/src/pi/__tests__/hooks.test.ts
  - packages/coda/src/pi/__tests__/commands.test.ts
  - packages/coda/src/tools/__tests__/coda-status.test.ts
  - packages/coda/src/tools/__tests__/coda-advance.test.ts
  - packages/coda/src/tools/__tests__/coda-back.test.ts
  - packages/coda/src/tools/__tests__/coda-kill.test.ts
  - packages/coda/src/workflow/build-loop.ts
  - packages/coda/src/workflow/context-builder.ts
  - packages/coda/src/workflow/phase-runner.ts
  - packages/coda/src/workflow/review-runner.ts
  - packages/coda/src/workflow/verify-runner.ts
  - packages/coda/src/pi/hooks.ts
  - packages/coda/src/pi/commands.ts
  - packages/coda/src/tools/coda-status.ts
  - packages/coda/src/tools/coda-advance.ts
  - packages/coda/src/tools/coda-back.ts
  - packages/coda/src/tools/coda-kill.ts
  - docs/v0.2/E2E-TEST-SCRIPT-v0.2.md
  - .paul/phases/15-e2e-validation/E2E-FINDINGS-v0.2.md
autonomous: true
---

<objective>
## Goal
Validate the four required v0.2 autonomous-loop scenarios end-to-end from the shipped workflow, tool, and Pi-facing surfaces, with reproducible evidence that the milestone closes cleanly on the existing v0.1 baseline.

## Purpose
Phases 9–14 landed the review/revise loop, verify/correct loop, human-review gate, exhaustion recovery controls, and Pi runtime integration. Phase 15 proves those capabilities work together as a real operator-facing system rather than only as isolated unit slices.

## Output
- Passing end-to-end regression coverage spanning workflow runners, operator tools, status output, and Pi hook/command surfaces
- A successful live CMUX/Pi execution of `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md` as the canonical Phase 15 validation runbook
- A findings report that records pass/fail evidence, any narrow fixes required to make the scenarios pass, and any explicitly deferred follow-up work
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
ROADMAP.md
pals.json
AGENTS.md

## Prior Work (only if genuinely needed)
.paul/phases/08-e2e-test/08-01-SUMMARY.md
.paul/phases/08-e2e-test/E2E-TEST-SCRIPT-v1.1.md
.paul/phases/08-e2e-test/E2E-FINDINGS-v1.1.md
.paul/phases/14-pi-integration-updates/14-01-SUMMARY.md

## Source-of-Truth / Reference Docs
docs/coda-spec-v7.md
README.md
docs/v0.2/E2E-TEST-SCRIPT-v0.2.md

## Source Files
packages/coda/src/workflow/review-runner.ts
packages/coda/src/workflow/verify-runner.ts
packages/coda/src/workflow/build-loop.ts
packages/coda/src/workflow/context-builder.ts
packages/coda/src/workflow/phase-runner.ts
packages/coda/src/pi/hooks.ts
packages/coda/src/pi/commands.ts
packages/coda/src/tools/coda-status.ts
packages/coda/src/tools/coda-advance.ts
packages/coda/src/tools/coda-back.ts
packages/coda/src/tools/coda-kill.ts
packages/coda/src/workflow/__tests__/review-runner.test.ts
packages/coda/src/workflow/__tests__/verify-runner.test.ts
packages/coda/src/workflow/__tests__/build-loop.test.ts
packages/coda/src/pi/__tests__/hooks.test.ts
packages/coda/src/pi/__tests__/commands.test.ts
packages/coda/src/tools/__tests__/coda-status.test.ts
packages/coda/src/tools/__tests__/coda-advance.test.ts
packages/coda/src/tools/__tests__/coda-back.test.ts
packages/coda/src/tools/__tests__/coda-kill.test.ts
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: arch(75) → monorepo / feature-layer structure detected under `packages/*`; planned work stays inside existing `packages/coda/src/{workflow,pi,tools}` test-and-integration surfaces plus phase-local findings artifacts, with no circular dependency concerns expected, and Phase 14’s moderate-size hotspots argue for extracting shared fixtures instead of further inflating `hooks.test.ts`, `commands.test.ts`, or `coda-status.test.ts`; todd(100) → Bun test infrastructure is present at repo and package level (`bun test` in root and package manifests), so end-to-end validation can be driven through executable tests before the live run; iris(150) → existing negative-path literals such as `HACKED` appear in prior tests/scripts by design, but no production anti-pattern markers are material to the planned files; gabe(175) → no external API-contract surface is primary in this phase; luke(185) → no UI files in scope; aria(190) → no UI files in scope; dana(195) → no schema or migration files in scope; dave(200) → no repo CI workflow files are currently tracked, so Phase 15 verification remains local-test plus live-CMUX driven; docs(200) → the canonical v0.2 runbook already exists at `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md`, so APPLY should execute that script and record findings instead of creating a parallel script copy; omar(205) → observability review is secondary here, but exhausted-state evidence should remain inspectable through persisted artifacts and status output; reed(210) → resilience focus is specifically the exhausted-loop/operator-recovery path already in scope; vera(215) → no privacy-sensitive files in scope; pete(225) → no heavy imports or performance-sensitive runtime paths are primary in this validation slice; ruby(250) → test debt risk is concentrated in the existing large test files, so shared fixture extraction is preferred over duplicative inline setup.

[dispatch] pre-plan enforcement: dean(50) → `bun audit --json` returned `{}` (no dependency findings); seth(80) → no hardcoded secret indicators surfaced in the planned code/test/doc paths.
</module_dispatch>

<acceptance_criteria>
## AC-1: Review / revise and human-review gating are validated end-to-end
```gherkin
Given a review-phase issue with either structural plan defects or a required human review gate
When the Phase 15 validation suite exercises the review lifecycle through workflow and Pi-facing surfaces
Then autonomous review writes revision instructions and enters `revise` for defective plans
And approved plans preserve the correct `human_review_status` and operator routing until a human decision is recorded
```

## AC-2: Verify / correct is validated end-to-end
```gherkin
Given a verify-phase issue with unmet acceptance criteria
When the Phase 15 validation suite exercises verification through workflow, build-loop, status, and Pi-facing surfaces
Then verification writes per-AC failure artifacts, creates correction tasks, and enters `correct`
And correction context exposes the failing AC, source-task lineage, and operator guidance needed to recover cleanly
```

## AC-3: Exhausted-loop recovery controls are validated end-to-end
```gherkin
Given review or verify loop iterations have reached the configured exhaustion limit
When the Phase 15 validation suite exercises exhausted states through status and operator controls
Then persisted revision or failure artifacts remain available for human recovery
And `/coda advance`, `/coda back`, `/coda kill`, and status messaging route the operator through the correct recovery path without hidden auto-advancement
```

## AC-4: Validation is reproducible from the current clean baseline
```gherkin
Given the Phase 15 scenarios and evidence artifacts are complete
When the targeted scenario suite, `bun test`, and `npx tsc --noEmit` are run from the current repo state
Then they pass without introducing regressions
And `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md` plus `.paul/phases/15-e2e-validation/E2E-FINDINGS-v0.2.md` capture the exact scenarios, commands, and outcomes needed to reproduce the validation
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Build automated scenario coverage that preflights the four required v0.2 flows before the live run</name>
  <files>packages/coda/src/workflow/__tests__/autonomous-loops-e2e.test.ts, packages/coda/src/workflow/__tests__/review-runner.test.ts, packages/coda/src/workflow/__tests__/verify-runner.test.ts, packages/coda/src/pi/__tests__/hooks.test.ts, packages/coda/src/pi/__tests__/commands.test.ts, packages/coda/src/tools/__tests__/coda-status.test.ts, packages/coda/src/tools/__tests__/coda-advance.test.ts, packages/coda/src/tools/__tests__/coda-back.test.ts, packages/coda/src/tools/__tests__/coda-kill.test.ts</files>
  <action>
    Add test-first Phase 15 coverage that exercises the milestone as integrated operator flows before running the live CMUX script:
    - Create a dedicated `autonomous-loops-e2e.test.ts` scenario suite that models the four required v0.2 paths from the canonical script: (1) review → revise, (2) verify → correct, (3) exhausted-loop recovery, and (4) human-review gate blocking BUILD while pending.
    - Reuse or extract shared fixture builders instead of duplicating large inline state setup in existing hotspot test files.
    - Extend the existing hook, command, status, and operator-control tests only where they need explicit assertions tying the scenario harness to Pi/runtime guidance and recovery behavior mirrored in the CMUX runbook.
    - Make the tests assert durable artifacts, submode transitions, human-review statuses, operator-visible next actions, and correction/recovery context so APPLY can catch obvious regressions before opening a live Pi session.
    Avoid writing shallow “calls function X” assertions; the evidence must prove the same scenario semantics the live operator run depends on.
  </action>
  <verify>bun test packages/coda/src/workflow/__tests__/autonomous-loops-e2e.test.ts packages/coda/src/workflow/__tests__/review-runner.test.ts packages/coda/src/workflow/__tests__/verify-runner.test.ts packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/tools/__tests__/coda-advance.test.ts packages/coda/src/tools/__tests__/coda-back.test.ts packages/coda/src/tools/__tests__/coda-kill.test.ts</verify>
  <done>AC-1 through AC-3 are represented by failing or incomplete automated scenario tests before any production fixes begin.</done>
</task>

<task type="auto">
  <name>Task 2: Close any narrow workflow, tool, or Pi integration gaps exposed by the scenario suite</name>
  <files>packages/coda/src/workflow/build-loop.ts, packages/coda/src/workflow/context-builder.ts, packages/coda/src/workflow/phase-runner.ts, packages/coda/src/workflow/review-runner.ts, packages/coda/src/workflow/verify-runner.ts, packages/coda/src/pi/hooks.ts, packages/coda/src/pi/commands.ts, packages/coda/src/tools/coda-status.ts, packages/coda/src/tools/coda-advance.ts, packages/coda/src/tools/coda-back.ts, packages/coda/src/tools/coda-kill.ts</files>
  <action>
    Fix only the concrete mismatches uncovered by the Phase 15 scenario harness:
    - Keep review/revise, verify/correct, human-review, and exhaustion semantics aligned with the shipped workflow model rather than inventing alternate control paths.
    - Preserve durable revision instructions, verification-failure artifacts, correction-task lineage, and exhausted-state evidence as the canonical recovery context.
    - Ensure Pi-facing hook/command/status output continues to reflect the same workflow-owned runtime metadata validated in the scenario suite.
    - If helper extraction from tests reveals a small integration bug in workflow or operator tools, patch the smallest responsible surface and immediately re-run the targeted suite.
    Avoid opportunistic redesign, new dependencies, non-validation docs cleanup, or unrelated refactors outside the scenario failures proved by Task 1.
  </action>
  <verify>bun test packages/coda/src/workflow/__tests__/autonomous-loops-e2e.test.ts packages/coda/src/workflow/__tests__/review-runner.test.ts packages/coda/src/workflow/__tests__/verify-runner.test.ts packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/tools/__tests__/coda-advance.test.ts packages/coda/src/tools/__tests__/coda-back.test.ts packages/coda/src/tools/__tests__/coda-kill.test.ts</verify>
  <done>AC-1 through AC-3 are satisfied by passing integrated workflow/tool/Pi scenario coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the canonical CMUX/Pi runbook and record Phase 15 findings</name>
  <files>docs/v0.2/E2E-TEST-SCRIPT-v0.2.md, .paul/phases/15-e2e-validation/E2E-FINDINGS-v0.2.md</files>
  <action>
    Produce reproducible validation evidence for this milestone closeout using the existing canonical v0.2 runbook:
    - Execute `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md` as a live CMUX/Pi validation run, not merely as documentation.
    - Record the executed results in `.paul/phases/15-e2e-validation/E2E-FINDINGS-v0.2.md`, including pass/fail evidence for each scripted scenario, any narrow fixes applied during the phase, and any explicitly deferred follow-up items that remain outside scope.
    - Run the targeted automated scenario suite before the live run, then `bun test` and `npx tsc --noEmit` after fixes and after the live run, and capture exact outcomes in the findings artifact.
    - If the canonical script itself needs correction to match shipped behavior discovered during APPLY, keep the edits narrow and treat `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md` as the source of truth rather than creating a second playbook elsewhere.
    Avoid vague narrative-only evidence; findings must tie directly to the live CMUX session and the repo verification commands.
  </action>
  <verify>bun test packages/coda/src/workflow/__tests__/autonomous-loops-e2e.test.ts packages/coda/src/workflow/__tests__/review-runner.test.ts packages/coda/src/workflow/__tests__/verify-runner.test.ts packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/tools/__tests__/coda-advance.test.ts packages/coda/src/tools/__tests__/coda-back.test.ts packages/coda/src/tools/__tests__/coda-kill.test.ts && bun test && npx tsc --noEmit</verify>
  <done>AC-4 is satisfied with an executed CMUX/Pi validation run plus a fully green repo-wide verification pass recorded in the findings artifact.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `packages/core/**` lifecycle primitives unless a proven Phase 15 validation failure cannot be fixed in `@coda/coda`
- README / CHANGELOG / broad docs cleanup unrelated to reproducing or explaining the Phase 15 validation evidence
- New architecture patterns, new persistence layers, or new dependencies
- The existing meaning of phase, submode, human-review, exhaustion, rewind, or kill semantics

## SCOPE LIMITS
- This plan validates the shipped v0.2 autonomous-loop behavior through both automated preflight tests and the canonical live CMUX/Pi runbook; it is not a new feature-design phase.
- Prefer shared test fixtures and narrow behavioral fixes over expanding already-large test files with copy-pasted setup.
- If validation exposes unrelated backlog work (for example the `@coda/core` symlink or broader Pi docs drift), capture it as findings/deferred work rather than widening APPLY.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `bun test packages/coda/src/workflow/__tests__/autonomous-loops-e2e.test.ts packages/coda/src/workflow/__tests__/review-runner.test.ts packages/coda/src/workflow/__tests__/verify-runner.test.ts packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/tools/__tests__/coda-advance.test.ts packages/coda/src/tools/__tests__/coda-back.test.ts packages/coda/src/tools/__tests__/coda-kill.test.ts`
- [ ] `bun test`
- [ ] `npx tsc --noEmit`
- [ ] Confirm the four required scenarios are explicitly covered: review→revise, verify→correct, exhausted-loop recovery, human-review gate blocking BUILD while pending
- [ ] Execute `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md` in a live CMUX/Pi session and record exact scenario outcomes in `.paul/phases/15-e2e-validation/E2E-FINDINGS-v0.2.md`
- [ ] Confirm revision instructions / verification-failure artifacts remain durable and are surfaced through status / command / hook context where applicable
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- All three tasks complete in order with test-first validation evidence
- The four required v0.2 scenarios have explicit, passing automated coverage and a successful live CMUX/Pi validation run
- Repo-wide tests and typecheck remain green
- Validation evidence is reproducible from `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md` and `.paul/phases/15-e2e-validation/E2E-FINDINGS-v0.2.md`
- No scope expansion beyond narrow fixes required to make the validated scenarios pass
</success_criteria>

<output>
After completion, create `.paul/phases/15-e2e-validation/15-01-SUMMARY.md`
</output>
