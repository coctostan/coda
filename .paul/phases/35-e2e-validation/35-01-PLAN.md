---
phase: 35-e2e-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v0.6/E2E-VALIDATION-RESULTS.md
autonomous: true
---

<objective>
**Goal:** Validate all v0.6 changes end-to-end: regression suite, VCS integration smoke test, coda_query smoke test.

**Purpose:** Confirm no regressions from phases 32–34 and validate the new capabilities work in realistic scenarios beyond unit tests.

**Output:** `docs/v0.6/E2E-VALIDATION-RESULTS.md` documenting all validation results.
</objective>

<context>
- v0.6 added: module cleanup (phase 32), VCS integration + /coda activate (phase 33), coda_query (phase 34)
- 460 tests (179 core + 281 coda), tsc clean
- 4 new files: vcs.ts, vcs.test.ts, coda-query.ts, coda-query.test.ts
- 10 Pi tools registered
- VCS: createBranch, commitTask, getCurrentBranch — tested with real temp git repos
- Query: codaQuery with 6 types + filters — tested with temp .coda structures
</context>

<acceptance_criteria>
- **AC-1:** Full regression suite passes (460 tests, 0 fail)
- **AC-2:** `tsc --noEmit` clean both packages
- **AC-3:** VCS smoke test: createBranch + commitTask work in a real git repo scenario
- **AC-4:** coda_query smoke test: query real .coda structure (if available) or confirm unit coverage is comprehensive
- **AC-5:** E2E validation results documented in `docs/v0.6/E2E-VALIDATION-RESULTS.md`
</acceptance_criteria>

<tasks>
<task id="1" title="Full regression + smoke tests + documentation">
<files>
- docs/v0.6/E2E-VALIDATION-RESULTS.md (NEW)
</files>
<action>
1. Run full regression suite for both packages
2. Run tsc --noEmit for both packages
3. VCS smoke test: verify VCS test suite covers realistic scenarios (branch creation, task commits, clean-tree no-ops, idempotent branch checkout)
4. Query smoke test: verify query test suite covers all 6 types, filtering, edge cases
5. Check that all v0.6 features are covered:
   - Module cleanup: old files removed, barrel re-exported
   - VCS: createBranch (5 issue types), commitTask (message format, no-op), getCurrentBranch
   - /coda activate: sets state, creates branch, error handling (4 cases)
   - Auto-commit: wired in coda_update for BUILD task completion
   - coda_query: 6 types, 3 filters, frontmatter-only
6. Create E2E validation results document
</action>
<verify>
Run: `cd packages/core && bun test` — 179 pass
Run: `cd packages/coda && bun test` — 281 pass
Run: `tsc --noEmit` — clean both
Confirm: docs/v0.6/E2E-VALIDATION-RESULTS.md exists with all results
</verify>
<done>AC-1 through AC-5 — all validation complete and documented</done>
</task>
</tasks>

<boundaries>
**DO NOT CHANGE:**
- Any source files in packages/
- This is validation only — no code changes

**SCOPE LIMITS:**
- No live CMUX test (regression suite + VCS unit tests provide sufficient coverage)
- Documentation only output
</boundaries>

<verification>
1. 460 tests pass, 0 fail
2. tsc clean both packages
3. E2E document created with comprehensive results
</verification>

<success_criteria>
- [ ] Regression passes (AC-1)
- [ ] TypeScript clean (AC-2)
- [ ] VCS coverage verified (AC-3)
- [ ] Query coverage verified (AC-4)
- [ ] Results documented (AC-5)
</success_criteria>

<module_dispatch>
Pre-plan: observational phase — all modules skip
</module_dispatch>

<output>
SUMMARY.md at `.paul/phases/35-e2e-validation/35-01-SUMMARY.md`
</output>
