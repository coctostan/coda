---
phase: 58-lifecycle-bug-fixes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/coda/src/tools/coda-advance.ts
  - packages/coda/src/tools/__tests__/coda-advance.test.ts
  - packages/core/src/data/sections.ts
  - packages/core/src/data/__tests__/sections.test.ts
  - packages/coda/src/tools/coda-edit-body.ts
  - packages/coda/src/tools/__tests__/coda-edit-body.test.ts
  - packages/coda/src/forge/scaffold.ts
  - packages/coda/src/forge/__tests__/scaffold.test.ts
  - packages/coda/src/tools/coda-forge.ts
  - packages/coda/src/tools/__tests__/coda-forge.test.ts
autonomous: true
---

<objective>
## Goal
Fix the three concrete lifecycle bugs that blocked the live v0.10 rerun: make `coda_advance` survive sparse SPECIFY frontmatter, stop `coda_edit_body` from silently duplicating equivalent section headings on replace, and make FORGE seed safe test-command defaults or an explicit follow-up instead of silent null ambiguity.

## Purpose
Phase 57 proved the remaining lifecycle failures are not abstract architecture problems; they are concrete, user-visible breakpoints in the real path through CODA. Phase 58 must remove those blockers without widening scope into VERIFY/UNIFY work, so Phase 59 can focus on lifecycle integrity rather than re-fighting basic PLAN/edit/scaffold breakage.

## Output
- `codaAdvance()` handles missing/partial issue frontmatter defensively in the SPECIFY → PLAN path and returns a gate failure instead of throwing.
- `codaEditBody()` / section replacement behavior matches the intended contract: replace existing/equivalent headings, reject ambiguous duplicates, and only append on explicit `create_if_missing` flows.
- FORGE scaffolding seeds test commands only when toolchain signals are conservative and compatible with current runner semantics; otherwise `coda_forge` explicitly tells the operator/agent to configure them via `coda_config`.
- Regression tests pin all three fixes and the repo test baseline stays green.
- `.paul/phases/58-lifecycle-bug-fixes/58-01-SUMMARY.md` at UNIFY reconciling shipped fixes, safety decisions, and verification evidence.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/milestones/v0.11.0-ROADMAP.md
.paul/phases/57-e2e-re-validation/57-01-SUMMARY.md

docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md         # live failure evidence for C1/C2/C3
docs/coda-spec-v7.md                              # `coda_edit_body` contract + TDD command expectations

## Prior Work
.paul/phases/57-e2e-re-validation/57-01-SUMMARY.md — Phase 57 is the direct handoff: entry improved, but SPECIFY → PLAN crashed, duplicated issue headings corrupted SPECIFY, and TDD remained unusable until commands were manually configured.

## Source Files
packages/coda/src/tools/coda-advance.ts                    # SPECIFY → PLAN gate data and transition handling
packages/coda/src/tools/__tests__/coda-advance.test.ts     # lifecycle transition regressions
packages/core/src/data/sections.ts                         # section parsing / replacement primitives
packages/core/src/data/__tests__/sections.test.ts          # section-level regression coverage
packages/coda/src/tools/coda-edit-body.ts                  # tool-level replace_section semantics
packages/coda/src/tools/__tests__/coda-edit-body.test.ts  # tool-level regression coverage
packages/coda/src/forge/scaffold.ts                        # default `coda.json` generation
packages/coda/src/forge/__tests__/scaffold.test.ts         # scaffold default coverage
packages/coda/src/tools/coda-forge.ts                      # explicit follow-up / next_action handoff after FORGE
packages/coda/src/tools/__tests__/coda-forge.test.ts      # forge output expectations
packages/coda/src/tools/coda-run-tests.ts                  # current command / pattern semantics that scaffold inference must respect
package.json                                               # repo test framework signal (`bun test`)
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
modules.yaml loaded from `/Users/maxwellnewman/.pi/agent/skills/pals/modules.yaml` (kernel_version 2.0.0)

**Advisory:**
- `[dispatch] pre-plan advisory: ARCH(75) → arch_context (detected pattern: layered monorepo, high confidence; scoped files stay within @coda/core data + @coda/coda tools/forge; structural_warning={packages/coda/src/tools/coda-advance.ts is already 669L, above the 500L soft threshold — keep the fix surgical and avoid using Phase 58 as a refactor vehicle}) | SETH(80) → sec_warnings=0 (no hardcoded secret patterns in scoped files; no auth-surface expansion planned) | TODD(100) → tdd_candidates={packages/coda/src/tools/__tests__/coda-advance.test.ts, packages/core/src/data/__tests__/sections.test.ts, packages/coda/src/tools/__tests__/coda-edit-body.test.ts, packages/coda/src/forge/__tests__/scaffold.test.ts, packages/coda/src/tools/__tests__/coda-forge.test.ts}, tdd_type=red→green | IRIS(150) → review_flags=0 in scoped files | DAVE(200) → no CI config detected in repo root; CI generation is a separate concern and out of scope for Phase 58 | DOCS(200) → doc_warnings={contract/reference docs already exist in docs/coda-spec-v7.md and docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md; README/spec sweep remains deferred in STATE.md follow-ups} | OMAR(205) → observability scope minimal; preserve current error-return shapes and do not widen logging behavior | CODI(220) → impact × 3 symbols → 3 resolved, 0 unresolved, 13 total call-sites, injected blast_radius | RUBY(250) → debt_flags={coda-advance.ts 669L, coda-advance.test.ts 802L; keep helpers local and avoid broad extraction in this phase}`
- `[dispatch] pre-plan advisory: skipped ARIA, LUKE, DANA, GABE, PETE, REED, VERA (no UI/data/API/perf/resilience/privacy files in scope)`

### Blast Radius (CODI)
**codaAdvance**
```text
packages/coda/src/pi/commands.ts:49:2b67  handleCodaCommand  behavioral  depth:1  [fan-in:1, fan-out:17, roles:none, coverage:untested, co-change:6.00, chain-confidence:0.90]
packages/coda/src/pi/commands.ts:29:1387  registerCommands  behavioral  depth:2  [fan-in:1, fan-out:2, roles:none, coverage:untested, co-change:6.00, chain-confidence:0.90]
packages/coda/src/workflow/__tests__/v08-e2e.test.ts:234:67d4  runCodaCommand  behavioral  depth:3  [fan-in:0, fan-out:2, roles:none, coverage:untested, co-change:2.00, chain-confidence:0.90]
packages/coda/src/pi/tools.ts:36:9276  registerTools  behavioral  depth:1  [fan-in:0, fan-out:15, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**codaEditBody**
```text
packages/coda/src/tools/coda-advance.ts:205:6a9a  codaAdvance  behavioral  depth:2  [fan-in:2, fan-out:19, roles:none, coverage:untested, co-change:2.00, chain-confidence:0.90]
packages/coda/src/tools/coda-advance.ts:337:0c17  handleHumanReviewDecision  behavioral  depth:1  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:2.00, chain-confidence:0.90]
packages/coda/src/tools/coda-advance.ts:414:06d4  handleUnifyReviewDecision  behavioral  depth:1  [fan-in:1, fan-out:3, roles:none, coverage:untested, co-change:2.00, chain-confidence:0.90]
packages/coda/src/pi/commands.ts:49:2b67  handleCodaCommand  behavioral  depth:3  [fan-in:1, fan-out:17, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
packages/coda/src/pi/tools.ts:36:9276  registerTools  behavioral  depth:1  [fan-in:0, fan-out:15, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**scaffoldCoda**
```text
packages/coda/src/tools/coda-forge.ts:5:ce4e  codaForge  behavioral  depth:1  [fan-in:2, fan-out:3, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
packages/coda/src/pi/commands.ts:49:2b67  handleCodaCommand  behavioral  depth:2  [fan-in:1, fan-out:17, roles:none, coverage:untested, co-change:5.00, chain-confidence:0.90]
packages/coda/src/pi/commands.ts:29:1387  registerCommands  behavioral  depth:3  [fan-in:1, fan-out:2, roles:none, coverage:untested, co-change:5.00, chain-confidence:0.90]
packages/coda/src/pi/tools.ts:36:9276  registerTools  behavioral  depth:2  [fan-in:0, fan-out:15, roles:entry-point, coverage:untested, co-change:3.00, chain-confidence:0.90]
```

**Enforcement:**
- `[dispatch] pre-plan enforcement: DEAN(50) → severity={critical:1, high:3, moderate:0, low:0}; .paul/dean-baseline.json (2026-04-17T00:54:41Z, staleness 30d) is still fresh; delta={new_critical:0, new_high:0}; proceed with warning but no new blocker.`

No remaining blockers. **Plan type: tdd** — all three fixes have crisp live regressions and existing test harness coverage, so RED must pin the broken behavior before GREEN changes production code.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
No SPECIAL-FLOWS.md configured for this project — skills section informational only.
</skills>

<acceptance_criteria>
## AC-1: SPECIFY → PLAN survives sparse issue frontmatter without changing gate semantics
```gherkin
Given a focused issue in `specify`
And its frontmatter is missing or only partially includes fields such as `acceptance_criteria`, `open_questions`, or `human_review`
When `coda_advance({ target_phase: "plan" })` runs
Then it does not throw `Cannot read properties of undefined (reading 'length')`
And missing AC data is treated conservatively as zero ACs / unresolved readiness
And the tool returns a normal gate failure or success result instead of crashing
And failed advances do not mutate the issue phase or `state.json`
```

## AC-2: replace_section no longer creates silent duplicate headings
```gherkin
Given a record body already contains the target section under the same or conservatively equivalent heading
When `coda_edit_body({ op: "replace_section", ... })` runs
Then the existing section is replaced instead of appending a duplicate heading
And if multiple equivalent headings exist, the tool returns an explicit error instead of guessing
And if no matching heading exists and `create_if_missing` is false, the tool returns an explicit error
And only explicit `create_if_missing: true` calls append a new section
And frontmatter remains unchanged for all edit paths
```

## AC-3: FORGE seeds only safe test-command defaults and otherwise gives an explicit follow-up
```gherkin
Given `scaffoldCoda()` / `coda_forge()` run in a workspace
When the project shape clearly indicates a safe test command compatible with current runner semantics
Then `.coda/coda.json` seeds `tdd_test_command` and `full_suite_command` with that default
And when the signals are ambiguous or unsafe
Then the commands remain unset rather than guessed incorrectly
And `coda_forge` returns a `next_action` that explicitly tells the operator/agent to configure test commands via `coda_config` before relying on TDD
```

## AC-4: Quality baseline holds and scope stays tight
```gherkin
Given all three tasks are complete
When the targeted regressions and full repo suite run
Then 0 tests fail
And no new dependencies are added
And no new TODO/FIXME/HACK/XXX markers appear in touched production files
And Phase 58 does not drift into VERIFY/UNIFY logic changes or the deferred docs sweep
And the existing DEAN baseline does not worsen during planning or implementation
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1 — Harden SPECIFY → PLAN gate data against missing frontmatter (RED → GREEN → REFACTOR)</name>
  <files>packages/coda/src/tools/coda-advance.ts, packages/coda/src/tools/__tests__/coda-advance.test.ts</files>
  <action>
    Start with RED in `coda-advance.test.ts`:
    - Add a failing regression that reproduces Phase 57's live crash by writing an issue record whose frontmatter omits or partially omits `acceptance_criteria`, `open_questions`, and/or `human_review`, then calling `codaAdvance({ target_phase: 'plan' })`.
    - Add assertions for both conservative failure and happy-path behavior: sparse/missing fields must fail cleanly without state mutation, while fully populated SPECIFY issues still advance normally.
    - Keep the existing successful/failed gate tests intact so the semantic gate remains `ready issue advances / incomplete issue blocks`.

    Move to GREEN in `coda-advance.ts`:
    - Contain all sparse-frontmatter handling inside small local helpers in `gatherGateData()` / nearby issue-frontmatter reads.
    - Default missing arrays to empty arrays and missing booleans/strings conservatively, but do not silently convert malformed readiness into success.
    - Audit adjacent optional-field reads in the same SPECIFY → PLAN path so no similar nil-unsafe `.length` / `.every` access remains.
    - Preserve the existing transition/state-update ordering: only write issue phase/frontmatter after `transition()` succeeds, and keep state persistence last.

    REFACTOR only if needed:
    - Extract a tiny issue-frontmatter normalization helper local to `coda-advance.ts`.
    - Do not use this phase to split `coda-advance.ts` into new files; the existing size debt is already tracked for later work.
  </action>
  <verify>bun test packages/coda/src/tools/__tests__/coda-advance.test.ts</verify>
  <done>AC-1 satisfied: SPECIFY → PLAN returns a real result instead of crashing on sparse issue frontmatter.</done>
</task>

<task type="auto">
  <name>Task 2 — Make replace_section deterministic and contract-safe (RED → GREEN → REFACTOR)</name>
  <files>packages/core/src/data/sections.ts, packages/core/src/data/__tests__/sections.test.ts, packages/coda/src/tools/coda-edit-body.ts, packages/coda/src/tools/__tests__/coda-edit-body.test.ts</files>
  <action>
    Start with RED in `sections.test.ts` and `coda-edit-body.test.ts`:
    - Add a failing regression mirroring the live issue corruption: repeated `replace_section` calls for headings like `Requirements`, `Acceptance Criteria`, and `Technical Considerations` must not append duplicate sections when an equivalent heading already exists.
    - Add failing tests for the contract edges documented in `docs/coda-spec-v7.md`: missing heading + `create_if_missing: false` returns an error, duplicate/equivalent headings return an explicit ambiguity error, and `create_if_missing: true` is the only append path.
    - Pin the normalization boundary tightly: allow conservative formatting equivalence only (case/whitespace/trailing-colon style noise), not fuzzy substring matching.

    Move to GREEN in `coda-edit-body.ts` and `sections.ts`:
    - Implement explicit section resolution for `replace_section`: exact match first, then one conservative normalized match, otherwise error.
    - Ensure the tool never silently falls back to append on plain replace requests.
    - Keep frontmatter untouched and preserve existing `append_section` / `append_text` behavior.
    - Update or add only the minimum core helper behavior needed so section boundaries remain deterministic and unit-testable.

    REFACTOR only if needed:
    - Keep heading normalization / duplicate detection in a tiny helper near the section-edit code.
    - Avoid broad markdown parsing work or new record/body abstractions in this phase.
  </action>
  <verify>bun test packages/core/src/data/__tests__/sections.test.ts packages/coda/src/tools/__tests__/coda-edit-body.test.ts</verify>
  <done>AC-2 satisfied: replace_section replaces the intended section, rejects ambiguous duplicates, and only appends when explicitly allowed.</done>
</task>

<task type="auto">
  <name>Task 3 — Seed conservative FORGE test-command defaults and explicit follow-up (RED → GREEN → REFACTOR)</name>
  <files>packages/coda/src/forge/scaffold.ts, packages/coda/src/forge/__tests__/scaffold.test.ts, packages/coda/src/tools/coda-forge.ts, packages/coda/src/tools/__tests__/coda-forge.test.ts</files>
  <action>
    Start with RED in `scaffold.test.ts` and `coda-forge.test.ts`:
    - Add failing scaffold tests for at least one safe-detection case (for example, Bun-signaled projects) and one ambiguous case where commands must stay null.
    - Add a failing forge test proving the ambiguous path is no longer silent: `next_action` must explicitly tell the operator/agent to configure `tdd_test_command` / `full_suite_command` with `coda_config` after choosing a toolchain.
    - If npm-style detection is proposed, pin the exact conditions tightly and only keep the test if the inferred command form is demonstrably compatible with current `coda_run_tests` pattern semantics.

    Move to GREEN in `scaffold.ts` and `coda-forge.ts`:
    - Teach `getDefaultConfig()` / `scaffoldCoda()` to inspect project-root signals conservatively (lockfile + package.json test-script evidence) and seed commands only when the runner semantics make that safe.
    - Leave ambiguous cases unset rather than guessing.
    - Update `coda_forge` greenfield/brownfield handoff wording so missing defaults become an explicit next step instead of a latent configuration trap.
    - Preserve the existing result shape and `.coda/coda.json` schema; do not add a new config field just to carry the follow-up text.

    REFACTOR only if needed:
    - Isolate detection logic in a small local helper inside `scaffold.ts`.
    - Do not widen scope into `forgeGreenfield()`, brownfield evidence generation, or `coda_run_tests` behavior unless a targeted regression proves it is necessary for this fix.
  </action>
  <verify>bun test packages/coda/src/forge/__tests__/scaffold.test.ts packages/coda/src/tools/__tests__/coda-forge.test.ts</verify>
  <done>AC-3 satisfied: FORGE either seeds a safe test command or says exactly what must be configured next.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- packages/coda/src/workflow/verify-runner.ts and packages/coda/src/workflow/unify-runner.ts (Phase 59 owns VERIFY/UNIFY integrity)
- docs/coda-spec-v7.md and README.md (spec/doc refresh is already deferred to milestone close)
- packages/coda/src/tools/write-gate.ts and write-gate perimeter code (Phase 55 already hardened this surface)
- `.coda/**` real project artifacts outside temporary test fixtures
- project architecture/layer boundaries or config schema shape beyond the minimal behavior needed for these three fixes

## SCOPE LIMITS
- No new dependencies
- No live CMUX / Script A rerun in this phase (that is Phase 60)
- No broad `coda-advance.ts` or `coda-advance.test.ts` refactor to solve the existing size warning
- No fuzzy natural-language heading matching; normalize only conservative formatting noise
- No VERIFY evidence policy or UNIFY artifact runner changes
- No docs/README sweep beyond the eventual SUMMARY.md
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `bun test packages/coda/src/tools/__tests__/coda-advance.test.ts packages/core/src/data/__tests__/sections.test.ts packages/coda/src/tools/__tests__/coda-edit-body.test.ts packages/coda/src/forge/__tests__/scaffold.test.ts packages/coda/src/tools/__tests__/coda-forge.test.ts`
- [ ] `bun test`
- [ ] `bun audit --json` (verify counts do not exceed the fresh baseline in .paul/dean-baseline.json)
- [ ] `grep -rn 'TODO\|FIXME\|HACK\|XXX' packages/coda/src/tools/coda-advance.ts packages/core/src/data/sections.ts packages/coda/src/tools/coda-edit-body.ts packages/coda/src/forge/scaffold.ts packages/coda/src/tools/coda-forge.ts`
- [ ] `wc -l packages/coda/src/tools/coda-advance.ts packages/core/src/data/sections.ts packages/coda/src/tools/coda-edit-body.ts packages/coda/src/forge/scaffold.ts packages/coda/src/tools/coda-forge.ts`
- [ ] `git diff --stat -- docs/coda-spec-v7.md README.md packages/coda/src/workflow/verify-runner.ts packages/coda/src/workflow/unify-runner.ts packages/coda/src/tools/write-gate.ts`
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks complete in RED → GREEN → REFACTOR order
- `coda_advance` no longer throws on sparse SPECIFY frontmatter and preserves conservative gate behavior
- `coda_edit_body` replace flows no longer leave duplicate headings in live-style issue records
- FORGE no longer leaves the agent/operator guessing about test-command setup
- Full repo test suite remains green without scope creep into VERIFY, UNIFY, docs, or write-gate work
</success_criteria>

<output>
After completion, create `.paul/phases/58-lifecycle-bug-fixes/58-01-SUMMARY.md`
</output>
