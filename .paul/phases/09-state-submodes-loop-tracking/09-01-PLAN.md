---
phase: 09-state-submodes-loop-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/state/types.ts
  - packages/core/src/state/machine.ts
  - packages/core/src/state/gates.ts
  - packages/core/src/state/store.ts
  - packages/core/src/state/__tests__/machine.test.ts
  - packages/core/src/state/__tests__/store.test.ts
  - packages/coda/src/forge/types.ts
  - packages/coda/src/forge/scaffold.ts
  - packages/coda/src/forge/__tests__/scaffold.test.ts
autonomous: true
---

<objective>
Goal: Add v0.2 state-level support for submodes, loop iteration tracking, exhaustion checks, and config defaults needed by later autonomous-loop phases.
Purpose: Establish the runtime and persistence primitives that REVIEW/VERIFY loop runners depend on, without implementing those runners yet.
Output: Updated core state schema/transition logic, updated gate data shape, v0.2 config defaults for max loop iterations, and passing state/config tests.
</objective>

<context>
Project brief:
- .paul/PROJECT.md
- .paul/ROADMAP.md
- .paul/STATE.md

Primary spec:
- docs/v0.2/01-submodes.md

Relevant source:
- packages/core/src/state/types.ts
- packages/core/src/state/machine.ts
- packages/core/src/state/gates.ts
- packages/core/src/state/store.ts
- packages/core/src/state/__tests__/machine.test.ts
- packages/core/src/state/__tests__/store.test.ts
- packages/coda/src/forge/types.ts
- packages/coda/src/forge/scaffold.ts
- packages/coda/src/forge/__tests__/scaffold.test.ts

Notes:
- This phase is limited to state/config primitives.
- Later phases own review-runner, verify-runner, human review, exhaustion UX, and Pi command behavior.
- Existing state-layer tests are present, so the plan uses a test-first task order while keeping plan type `execute`.
</context>

<module_dispatch>
Pre-plan advisory:
- TODD(100): Existing Bun test coverage detected for state and forge config files.
- IRIS(150): No TODO/FIXME/HACK/XXX markers in planned files.
- DAVE(200): No CI config detected in repo root; advisory only, out of scope for this phase.
- DOCS(200): Phase has direct spec coverage in `docs/v0.2/01-submodes.md`; forge config files have no nearby dedicated doc, acceptable for this scoped config extension.
- RUBY(250): No preliminary debt indicators from planned file size/shape.

Pre-plan enforcement:
- DEAN(50): `npm audit` unavailable without package-lock; `bun audit --json` returned empty result. No actionable dependency block for this phase.
- SETH(80): No secret/auth/security concerns apparent in planned files.
- ARCH(75): Planned changes stay within `packages/core/src/state/` plus `.coda` config defaults in forge; no architectural boundary concern at plan time.

Post-plan suggestion:
- TODD would support a TDD-style plan because tests already exist for the target area. This plan preserves `execute` type but starts with failing test additions.
</module_dispatch>

<acceptance_criteria>
AC-1
Given the v0.2 state spec
When `createDefaultState()` or state persistence is used
Then `CodaState` includes `submode` and `loop_iteration` fields with defaults compatible with v0.2 and existing state behavior remains intact.

AC-2
Given a state in REVIEW or VERIFY
When valid submode transitions occur
Then submode transitions are validated correctly, loop iteration increments only on `revise -> review` and `correct -> verify`, and invalid transitions are rejected.

AC-3
Given phase transitions into and out of REVIEW/VERIFY
When `transition()` is called for those phases
Then entry sets the correct submode and zeroes loop iteration, and exit clears submode and resets loop iteration.

AC-4
Given a CODA config and a state in REVIEW or VERIFY
When loop exhaustion is checked
Then the implementation can determine exhaustion using `max_review_iterations` / `max_verify_iterations` with default value `3`.

AC-5
Given `.coda/coda.json` defaults are scaffolded
When a new project is forged
Then the config includes `max_review_iterations` and `max_verify_iterations` defaults without regressing existing config structure.

AC-6
Given the updated implementation
When the targeted test suite and typecheck run
Then state tests and forge config tests pass and TypeScript remains clean.
</acceptance_criteria>

<tasks>
Task 1: Add failing tests for v0.2 state and config behavior
<files>
- packages/core/src/state/__tests__/machine.test.ts
- packages/core/src/state/__tests__/store.test.ts
- packages/coda/src/forge/__tests__/scaffold.test.ts
</files>
<action>
Add explicit test coverage for:
- default `submode: null` and `loop_iteration: 0`
- review/verify phase entry initialization
- valid/invalid submode transitions
- loop iteration increment rules
- reset behavior on exit from review/verify
- persisted load/save of the new state fields
- default scaffold config values for `max_review_iterations` and `max_verify_iterations`
Make the new assertions fail against the current implementation before implementation tasks proceed.
</action>
<verify>
- bun test packages/core/src/state/__tests__/machine.test.ts packages/core/src/state/__tests__/store.test.ts packages/coda/src/forge/__tests__/scaffold.test.ts
</verify>
<done>
- New tests exist and encode AC-1 through AC-5 expectations
- At least part of the new suite fails against v0.1 implementation, proving the phase change is real
</done>

Task 2: Implement v0.2 state schema and transition behavior in core
<files>
- packages/core/src/state/types.ts
- packages/core/src/state/machine.ts
- packages/core/src/state/gates.ts
- packages/core/src/state/store.ts
</files>
<action>
Update core state primitives to match `docs/v0.2/01-submodes.md`:
- extend `CodaState` with `submode` and `loop_iteration`
- add `Submode` type
- extend gate-check input shape for upcoming human-review gating needs if required by this phase boundary
- implement submode-aware phase entry/exit handling inside `transition()`
- add explicit submode transition and exhaustion helpers in the state layer as needed by the spec
- preserve existing linear phase behavior and existing gate semantics outside the new review/verify state behavior
- keep persistence logic compatible with the extended state schema
</action>
<verify>
- bun test packages/core/src/state/__tests__/machine.test.ts packages/core/src/state/__tests__/store.test.ts packages/core/src/state/__tests__/gates.test.ts
</verify>
<done>
- AC-1 through AC-4 are implemented in core state code
- Existing v0.1 state tests continue to pass alongside new v0.2 coverage
</done>

Task 3: Extend `.coda` config defaults for loop exhaustion settings
<files>
- packages/coda/src/forge/types.ts
- packages/coda/src/forge/scaffold.ts
- packages/coda/src/forge/__tests__/scaffold.test.ts
</files>
<action>
Update the forge config type and default scaffolded config to include:
- `max_review_iterations`
- `max_verify_iterations`
with default value `3`, while preserving existing config keys and test expectations.
</action>
<verify>
- bun test packages/coda/src/forge/__tests__/scaffold.test.ts
</verify>
<done>
- AC-5 is satisfied
- Scaffolding still produces valid `coda.json` output with prior keys intact
</done>
</tasks>

<boundaries>
DO NOT CHANGE:
- review-runner / verify-runner implementation
- build-loop or phase-runner orchestration beyond what is strictly required for state typing in this phase
- Pi commands, hooks, or human intervention flows
- issue/task record schemas beyond what this phase explicitly requires for state/config primitives

SCOPE LIMITS:
- Limit runtime changes to the state engine and forge config defaults required by `docs/v0.2/01-submodes.md`
- Do not implement exhaustion UX, pause/resume behavior, or correction task logic yet
- Do not normalize artifact formats or redesign config layout
</boundaries>

<verification>
Run after implementation:
- bun test packages/core/src/state/__tests__/gates.test.ts packages/core/src/state/__tests__/machine.test.ts packages/core/src/state/__tests__/store.test.ts
- bun test packages/coda/src/forge/__tests__/scaffold.test.ts
- npx tsc --noEmit

Manual review checks:
- `transition()` behavior still enforces linear phase order
- review/verify entry and exit semantics match the spec exactly
- loop iteration increments only on re-check transitions, not on first entry
- config defaults are present and typed without breaking existing scaffold behavior
</verification>

<success_criteria>
- Phase 9 plan stays confined to state/config primitives needed by later autonomous-loop phases
- New tests cover all state-submode semantics called out in `docs/v0.2/01-submodes.md`
- Implementation introduces no regressions to existing v0.1 state behavior
- Targeted tests and repository typecheck pass
</success_criteria>

<output>
Summary file to produce after APPLY:
- .paul/phases/09-state-submodes-loop-tracking/09-01-SUMMARY.md
</output>
