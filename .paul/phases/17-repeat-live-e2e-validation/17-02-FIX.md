---
phase: 17-repeat-live-e2e-validation
plan: 02
type: fix
wave: 1
depends_on: []
files_modified: []
autonomous: true
---

<objective>
## Fix
Normalize optional array fields on TaskRecord at the `loadTasks()` boundary so all downstream runners can trust the arrays exist. Add regression tests exercising tasks with missing optional fields.
</objective>

<tasks>

<task type="auto">
  <name>Fix: Normalize optional TaskRecord arrays in loadTasks()</name>
  <files>packages/coda/src/workflow/context-builder.ts</files>
  <action>
    In `loadTasks()`, after reading each task record, default all optional array fields to `[]`:
    - `covers_ac ?? []`
    - `depends_on ?? []`
    - `files_to_modify ?? []`
    - `truths ?? []`
    - `artifacts ?? []`
    - `key_links ?? []`
    This single normalization protects all 5 crash sites in review-runner and verify-runner.
  </action>
  <verify>npx tsc --noEmit && bun test</verify>
  <done>Fix verified working</done>
</task>

<task type="auto">
  <name>Fix: Add regression tests for tasks missing optional array fields</name>
  <files>packages/coda/src/workflow/__tests__/review-runner.test.ts, packages/coda/src/workflow/__tests__/verify-runner.test.ts</files>
  <action>
    Add test cases where task records omit `depends_on`, `covers_ac`, and `files_to_modify`.
    Verify the review runner and verify runner handle them without crashing.
  </action>
  <verify>bun test packages/coda/src/workflow/__tests__/review-runner.test.ts packages/coda/src/workflow/__tests__/verify-runner.test.ts</verify>
  <done>Fix verified working</done>
</task>

</tasks>

<verification>
- [ ] Fix applied and verified
- [ ] No regressions introduced
</verification>
