---
phase: 17-repeat-live-e2e-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v0.2/E2E-TEST-SCRIPT-v0.2.md
  - .paul/phases/17-repeat-live-e2e-validation/E2E-FINDINGS-v0.2.md
  - .paul/phases/17-repeat-live-e2e-validation/CMUX-TRANSCRIPT-v0.2.md
autonomous: true
---

<objective>
## Goal
Re-run the canonical v0.2 CMUX/Pi validation from a clean baseline and capture a fully green live transcript plus final findings that close the milestone on live evidence, not just automated tests.

## Purpose
Phase 15 proved the autonomous-loop system in automated coverage but stopped short of a clean live closeout, and Phase 16 fixed the supported operator trigger path. Phase 17 exists to prove that the shipped `coda_advance` + extension-managed follow-up behavior now works end-to-end in a fresh live session without reopening implementation scope.

## Output
- A clean live CMUX/Pi execution of `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md`
- `.paul/phases/17-repeat-live-e2e-validation/CMUX-TRANSCRIPT-v0.2.md` with the exact transcript/evidence
- `.paul/phases/17-repeat-live-e2e-validation/E2E-FINDINGS-v0.2.md` with scenario-by-scenario results, baseline verification, and milestone closeout status
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
pals.json
AGENTS.md

## Prior Work (only if genuinely needed)
.paul/phases/15-e2e-validation/15-01-SUMMARY.md
.paul/phases/15-e2e-validation/E2E-FINDINGS-v0.2.md
.paul/phases/16-live-operator-trigger-resolution/16-01-SUMMARY.md

## Source-of-Truth / Reference Docs
docs/v0.2/E2E-TEST-SCRIPT-v0.2.md
docs/coda-spec-v7.md

## Source Files
packages/coda/src/pi/commands.ts
packages/coda/src/pi/hooks.ts
packages/coda/src/workflow/review-runner.ts
packages/coda/src/workflow/verify-runner.ts
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: todd(100) → Bun test infrastructure is present at the repo root (`bun test`) and across `packages/*/__tests__`, so Phase 17 can preflight the clean baseline with the existing targeted trigger suite plus the full repo suite before the live rerun; iris(150) → no actionable TODO/FIXME/HACK/XXX markers surfaced in the canonical runbook, with the only match being the intentional `mktemp -d -t coda-v02-e2e-XXXX` suffix placeholder; dave(200) → no tracked CI workflow files were found, so verification remains local (`bun test`, targeted trigger suite, `npx tsc --noEmit`) plus the live CMUX/Pi run; docs(200) → the canonical v0.2 runbook already exists at `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md`, so APPLY should execute it in place and record transcript/findings instead of creating a parallel script; ruby(250) → the runbook is already 453 lines, so keep Phase 17 changes limited to findings/transcript and only the smallest literal runbook correction if the executed live rerun proves one is necessary.
[dispatch] pre-plan enforcement: dean(50) → PASS; `bun audit --json` returned `{}` so there is no dependency block for planning.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
No specialized flows configured.
</skills>

<acceptance_criteria>
## AC-1: Review/revise and human-review gating pass in the live rerun
```gherkin
Given a fresh CMUX/Pi session started from the current Phase 16 branch tip and the canonical v0.2 runbook
When the live rerun exercises the flawed-plan review flow and the pending human-review gate
Then `coda_advance` plus the extension-managed follow-up turn drives review into `revise`, writes revision instructions, returns to `review`, and blocks BUILD until a human approval is recorded
```

## AC-2: Verify/correct and recovery controls pass in the live rerun
```gherkin
Given the same fresh live validation session
When the runbook exercises unmet-AC verification, correction, exhaustion/recovery behavior, and `/coda back` preservation
Then verify enters `correct` with durable failure artifacts, correction work returns to `verify`, exhausted automation pauses visibly, and rewind/kill flows preserve or close issue artifacts exactly as documented
```

## AC-3: Final live evidence closes the milestone without widening scope
```gherkin
Given the targeted trigger regressions and repo-wide verification are already green on the current branch
When Phase 17 records the clean rerun transcript and final findings
Then `.paul/phases/17-repeat-live-e2e-validation/CMUX-TRANSCRIPT-v0.2.md` and `E2E-FINDINGS-v0.2.md` capture the exact commands, outputs, artifact checks, and pass/fail outcomes needed to prove the milestone is closed on both automated and live evidence
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Reconfirm the clean automated baseline and stage the Phase 17 evidence artifacts</name>
  <files>.paul/phases/17-repeat-live-e2e-validation/E2E-FINDINGS-v0.2.md, .paul/phases/17-repeat-live-e2e-validation/CMUX-TRANSCRIPT-v0.2.md</files>
  <action>
    Treat the current Phase 16 tip as the only supported baseline for the live rerun.
    - Before opening a CMUX session, run the targeted trigger regression suite from Phase 16, then `bun test`, then `npx tsc --noEmit`.
    - Record the exact branch/commit, verification commands, and results at the top of the Phase 17 findings artifact so the live run is anchored to a proven clean baseline.
    - Seed the transcript artifact with the environment assumptions and exact launch/bootstrap commands from the canonical runbook.
    - If the automated baseline is not green, stop Phase 17 as blocked and record the blocker instead of patching unrelated code or backlogs inside this validation phase.
  </action>
  <verify>bun test packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/workflow/__tests__/autonomous-loops-e2e.test.ts && bun test && npx tsc --noEmit</verify>
  <done>AC-3 satisfied in part: the live rerun starts from a recorded clean automated baseline with dedicated Phase 17 evidence artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the canonical live CMUX/Pi rerun from a fresh temp project and capture the transcript</name>
  <files>.paul/phases/17-repeat-live-e2e-validation/CMUX-TRANSCRIPT-v0.2.md, .paul/phases/17-repeat-live-e2e-validation/E2E-FINDINGS-v0.2.md</files>
  <action>
    Run `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md` end-to-end against a fresh temp project and a live `pi -e packages/coda/src/pi/index.ts` session, using the supported trigger contract established in Phase 16.
    - Capture the exact CMUX commands, wait points, screen reads, and filesystem/state checks in the transcript artifact rather than summarizing them from memory later.
    - Verify each scripted checkpoint: review → revise, re-review approval, pending human-review gate, verify → correct, re-verify, exhaustion messaging, `/coda kill`, and `/coda back` preservation.
    - Use only the documented `coda_advance` + extension-managed follow-up path to enter review/verify automation; do not invoke hidden internal runner steps.
    - If the harness still prevents a clean rerun, stop at the exact failing step, preserve the transcript, and record the narrow blocker in findings instead of reopening Phase 16 implementation scope inside this plan.
  </action>
  <verify>Transcript shows the canonical runbook was executed from a fresh temp directory and includes the exact screen/output evidence for each scripted checkpoint plus filesystem checks for revision instructions, verification-failure artifacts, correction tasks, and rewind preservation.</verify>
  <done>AC-1 and AC-2 are satisfied by a clean live rerun, or a single exact blocker is captured with full transcript evidence if the rerun still fails.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize Phase 17 findings and only make a minimal runbook correction if the executed rerun proves one is required</name>
  <files>docs/v0.2/E2E-TEST-SCRIPT-v0.2.md, .paul/phases/17-repeat-live-e2e-validation/E2E-FINDINGS-v0.2.md, .paul/phases/17-repeat-live-e2e-validation/CMUX-TRANSCRIPT-v0.2.md</files>
  <action>
    Turn the live transcript into the final Phase 17 validation record.
    - Populate the findings artifact with a scenario-by-scenario pass/fail table, baseline verification results, exact artifact locations, deviations, and the milestone closeout recommendation.
    - If the live rerun proves a literal mismatch in `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md`, patch only that exact documented step and note the correction in findings; otherwise leave the runbook unchanged.
    - Re-read the final findings and transcript together to ensure they are sufficient for UNIFY without relying on unstated session memory.
    - Avoid production-code changes, portability cleanup (`@coda/core` symlink, `modules.yaml` handling), or broader docs drift work; record any such items as deferred follow-up only.
  </action>
  <verify>Read back `.paul/phases/17-repeat-live-e2e-validation/E2E-FINDINGS-v0.2.md` and `.paul/phases/17-repeat-live-e2e-validation/CMUX-TRANSCRIPT-v0.2.md` to confirm they contain baseline commands, live scenario results, artifact proofs, and the final milestone recommendation; if the runbook was edited, re-read `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md` to confirm the change is limited to the proven mismatch.</verify>
  <done>AC-3 satisfied: Phase 17 produces the durable live evidence needed to close v0.2 without widening scope.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `packages/core/**` and `packages/coda/src/**` runtime behavior unless the live rerun reveals a new blocker that cannot be documented without a separate follow-on phase
- The supported trigger contract established in Phase 16 (`coda_advance` into `review`/`verify` plus extension-managed follow-up turns)
- Unrelated backlog items: `@coda/core` symlink portability, repo-root `modules.yaml` portability, and broader Pi docs alignment work outside the canonical v0.2 runbook

## SCOPE LIMITS
- Phase 17 is a validation-and-evidence phase, not another implementation phase
- Prefer findings/transcript capture over code churn; only a minimal literal runbook correction is in scope if the executed rerun proves one is necessary
- If the live rerun still fails, capture the exact blocker and stop; do not reopen Phase 16 trigger implementation or widen into unrelated cleanup inside APPLY
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `bun test packages/coda/src/pi/__tests__/commands.test.ts packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/workflow/__tests__/autonomous-loops-e2e.test.ts`
- [ ] `bun test`
- [ ] `npx tsc --noEmit`
- [ ] Execute `docs/v0.2/E2E-TEST-SCRIPT-v0.2.md` in a fresh temp project with a live CMUX/Pi session and capture the exact transcript in `.paul/phases/17-repeat-live-e2e-validation/CMUX-TRANSCRIPT-v0.2.md`
- [ ] Confirm the live rerun explicitly covers review→revise, human-review gate, verify→correct, exhaustion pause, `/coda kill`, and `/coda back` preservation
- [ ] Confirm `.paul/phases/17-repeat-live-e2e-validation/E2E-FINDINGS-v0.2.md` records exact artifact evidence and a final milestone closeout recommendation
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- The canonical live runbook is executed from a clean baseline without hidden operator knowledge
- Phase 17 captures a durable CMUX transcript and final findings artifact tied to exact commands and outputs
- Automated baseline verification remains green on the same branch tip used for the live rerun
- No scope expansion occurs beyond evidence capture and a minimal proven runbook correction if needed
</success_criteria>

<output>
After completion, create `.paul/phases/17-repeat-live-e2e-validation/17-01-SUMMARY.md`
</output>
