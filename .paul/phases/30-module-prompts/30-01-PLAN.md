---
phase: 30-module-prompts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/modules/registry.ts
  - modules/prompts/architecture/pre-plan.md
  - modules/prompts/architecture/post-build.md
  - modules/prompts/quality/pre-build.md
  - modules/prompts/quality/post-build.md
  - modules/prompts/quality/post-unify.md
  - modules/prompts/knowledge/post-build.md
  - modules/prompts/knowledge/post-unify.md
autonomous: true
---

<objective>
## Goal
Add the 3 remaining modules (architecture, quality, knowledge) to the CODA module system — 7 prompt files and 3 registry definitions. After this, all 5 designed modules are active.

## Purpose
Completes the v0.3.1 module expansion from the spec. Architecture checks structural boundaries, quality tracks regression baselines, and knowledge captures decisions/patterns. All infrastructure is already built — this is prompt files + registry entries only.

## Output
- 3 new MODULE_DEFINITIONS entries in registry.ts (architecture, quality, knowledge)
- 7 new prompt markdown files in modules/prompts/
- Existing data-driven prompts.test.ts auto-generates 35 structural tests (was 25 for 5 files, now 35 for 12 files)
- 5 modules total: security (2 prompts), tdd (3 prompts), architecture (2 prompts), quality (3 prompts), knowledge (2 prompts)
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Spec
docs/v0.3/04-module-prompts.md

## Source Files
packages/core/src/modules/registry.ts
packages/core/src/modules/__tests__/prompts.test.ts
modules/prompts/security/pre-plan.md (convention reference)
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: TODD(100) → data-driven prompts.test.ts auto-generates tests from MODULE_DEFINITIONS | IRIS(150) → 0 | DOCS(200) → spec doc exists | RUBY(250) → skip
[dispatch] pre-plan enforcement: DEAN(50) → skip | ARCH(75) → no concerns (markdown data files + L3 registry) | SETH(80) → no secrets
</module_dispatch>

<acceptance_criteria>

## AC-1: Architecture module registered with correct hooks
```gherkin
Given MODULE_DEFINITIONS in registry.ts
When the architecture module is looked up
Then it has name "architecture", domain "Architecture Patterns", hooks at pre-plan(75) and post-build(125), and promptFile paths "architecture/pre-plan.md" and "architecture/post-build.md"
```

## AC-2: Quality module registered with correct hooks
```gherkin
Given MODULE_DEFINITIONS in registry.ts
When the quality module is looked up
Then it has name "quality", domain "Quality Assurance", hooks at pre-build(100), post-build(100), and post-unify(100), and promptFile paths "quality/pre-build.md", "quality/post-build.md", "quality/post-unify.md"
```

## AC-3: Knowledge module registered with correct hooks
```gherkin
Given MODULE_DEFINITIONS in registry.ts
When the knowledge module is looked up
Then it has name "knowledge", domain "Knowledge Capture", hooks at post-build(300) and post-unify(200), and promptFile paths "knowledge/post-build.md" and "knowledge/post-unify.md"
```

## AC-4: Default thresholds set correctly
```gherkin
Given DEFAULT_THRESHOLDS in registry.ts
When architecture, quality, and knowledge thresholds are checked
Then architecture = "high", quality = "high", knowledge = "none"
```

## AC-5: All 7 prompt files follow the convention
```gherkin
Given the 7 new prompt files in modules/prompts/
When each file is read
Then each contains the module name or domain, a "What to Check" section, a "Severity Guide" section, and an "Assumption Guidance" section
```

## AC-6: Structural tests pass for all 12 prompt files
```gherkin
Given the data-driven prompts.test.ts reads MODULE_DEFINITIONS
When bun test runs prompts.test.ts
Then all tests pass for all 12 prompt files (5 existing + 7 new) across 5 test categories
```

## AC-7: No regressions
```gherkin
Given the existing test suite of 398 tests
When all changes are applied
Then all tests pass and tsc --noEmit is clean for both packages
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Registry definitions + prompt files</name>
  <files>packages/core/src/modules/registry.ts, modules/prompts/architecture/pre-plan.md, modules/prompts/architecture/post-build.md, modules/prompts/quality/pre-build.md, modules/prompts/quality/post-build.md, modules/prompts/quality/post-unify.md, modules/prompts/knowledge/post-build.md, modules/prompts/knowledge/post-unify.md</files>
  <action>
    **Registry — packages/core/src/modules/registry.ts:**
    1. Add 3 new entries to MODULE_DEFINITIONS after the `tdd` entry:

       ```typescript
       architecture: {
         name: 'architecture',
         domain: 'Architecture Patterns',
         version: '1.0.0',
         hooks: [
           { hookPoint: 'pre-plan', priority: 75, promptFile: 'architecture/pre-plan.md' },
           { hookPoint: 'post-build', priority: 125, promptFile: 'architecture/post-build.md' },
         ],
       },
       quality: {
         name: 'quality',
         domain: 'Quality Assurance',
         version: '1.0.0',
         hooks: [
           { hookPoint: 'pre-build', priority: 100, promptFile: 'quality/pre-build.md' },
           { hookPoint: 'post-build', priority: 100, promptFile: 'quality/post-build.md' },
           { hookPoint: 'post-unify', priority: 100, promptFile: 'quality/post-unify.md' },
         ],
       },
       knowledge: {
         name: 'knowledge',
         domain: 'Knowledge Capture',
         version: '1.0.0',
         hooks: [
           { hookPoint: 'post-build', priority: 300, promptFile: 'knowledge/post-build.md' },
           { hookPoint: 'post-unify', priority: 200, promptFile: 'knowledge/post-unify.md' },
         ],
       },
       ```

    2. Add 3 new entries to DEFAULT_THRESHOLDS:
       ```typescript
       architecture: 'high',
       quality: 'high',
       knowledge: 'none',
       ```

    3. Update the JSDoc comments to reflect "v0.3.1 ships 5 module definitions" (was "v0.3 ships 2").

    **Prompt Files — Follow the exact convention from `security/pre-plan.md`:**

    Each prompt file MUST have:
    - `## {Module Name} — {Hook Point Description}` header
    - `### Context` section
    - `### What to Check` section with numbered checklist
    - `### Severity Guide` section with CRITICAL/HIGH/MEDIUM/LOW/INFO
    - `### Assumption Guidance` section

    Write the content per docs/v0.3/04-module-prompts.md spec sections:

    **architecture/pre-plan.md:** Layer boundary checks, file placement, circular dependency risk, god file size
    **architecture/post-build.md:** Import direction verification, file size growth, responsibility creep, new circular deps
    **quality/pre-build.md:** Baseline capture — run test suite, lint, typecheck, record counts
    **quality/post-build.md:** Regression check — re-run suite, compare against baseline, flag regressions
    **quality/post-unify.md:** Quality delta — compute before/after, record trend (improving/stable/declining), produce markdown table
    **knowledge/post-build.md:** Decision capture — undocumented design decisions, trade-offs, new conventions
    **knowledge/post-unify.md:** Compound extraction — patterns, conventions to document, decision records to create, lessons learned

    **Severity guides per module:**
    - Architecture: CRITICAL=circular deps, HIGH=layer violations, MEDIUM=file >500 lines, LOW=unexpected location
    - Quality: HIGH=test regression or new type errors, MEDIUM=new lint warnings, LOW=test count didn't increase, INFO=quality summary
    - Knowledge: INFO only (knowledge module never blocks)

    **Avoid:**
    - Do NOT include the JSON output requirement in prompts — the dispatcher appends that automatically
    - Do NOT modify any infrastructure (dispatcher, types, schema) — only registry.ts and prompt files
    - Do NOT modify existing security or tdd prompts
  </action>
  <verify>
    cd packages/core && bun test src/modules/__tests__/prompts.test.ts — all structural tests pass for 12 prompt files
    cd packages/core && bun test — all 144+ tests pass
    cd packages/core && npx tsc --noEmit — clean
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4, AC-5, AC-6 satisfied: all 3 modules registered, all 7 prompts follow convention, structural tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Full regression verification</name>
  <files>packages/core, packages/coda</files>
  <action>
    1. Run the full test suite for both packages:
       - `cd packages/core && bun test`
       - `cd packages/coda && bun test`
    2. Run TypeScript checks:
       - `cd packages/core && npx tsc --noEmit`
       - `cd packages/coda && npx tsc --noEmit`
    3. Verify test counts: core should have 144+ (may gain tests from data-driven prompt tests), coda should remain 254.
    4. Verify total 5 modules × expected hooks = 12 total prompt files.
    5. If any failures, fix without changing acceptance criteria.
  </action>
  <verify>
    bun test (core) → all pass, 0 fail
    bun test (coda) → 254 pass, 0 fail
    tsc --noEmit → clean (both packages)
    ls modules/prompts/ — 5 directories, 12 .md files (excluding legacy todd.md/walt.md)
  </verify>
  <done>AC-7 satisfied: no regressions, all tests pass, tsc clean</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- packages/core/src/modules/types.ts (HookPoint type already has all needed hooks)
- packages/core/src/modules/dispatcher.ts (infrastructure stable)
- packages/core/src/modules/finding-schema.ts (schema stable)
- packages/core/src/modules/__tests__/prompts.test.ts (data-driven, auto-adapts)
- packages/coda/src/** (no coda changes in this phase)
- modules/prompts/security/** (existing prompts stable)
- modules/prompts/tdd/** (existing prompts stable)

## SCOPE LIMITS
- Registry definitions + prompt markdown files only
- No TypeScript infrastructure changes
- No external dependencies
- No changes to the dispatcher, types, schema, or existing modules

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `bun test` passes in packages/core (all tests including new prompt structural tests)
- [ ] `bun test` passes in packages/coda (254 tests)
- [ ] `npx tsc --noEmit` clean in packages/core
- [ ] `npx tsc --noEmit` clean in packages/coda
- [ ] 5 module directories in modules/prompts/ (security, tdd, architecture, quality, knowledge)
- [ ] 12 prompt .md files total across all 5 modules
- [ ] All prompt files have: module name, What to Check, Severity Guide, Assumption Guidance
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 5 modules registered with correct hooks and thresholds
- 12 prompt files following convention
- No regressions
</success_criteria>

<output>
After completion, create `.paul/phases/30-module-prompts/30-01-SUMMARY.md`
</output>
