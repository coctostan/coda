---
phase: 32-module-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/prompts/todd.md
  - modules/prompts/walt.md
  - packages/coda/src/modules/index.ts
  - packages/coda/src/modules/__tests__/modules.test.ts
  - packages/coda/src/index.ts
autonomous: true
---

<objective>
**Goal:** Remove legacy module artifacts that were superseded by the v0.3 registry-based module system.

**Purpose:** The v0.3 module system replaced old flat prompt files (`todd.md`, `walt.md`) with a subdirectory structure (`modules/prompts/{module}/`) and replaced the old TS module loaders with a registry + dispatcher. Two legacy artifacts remain:
1. `modules/prompts/todd.md` and `modules/prompts/walt.md` — old flat prompt files sitting alongside the new subdirectory structure
2. `packages/coda/src/modules/` — a backward-compat barrel re-exporting from `workflow/module-integration.ts`, plus its test

**Output:** Clean module tree with no legacy remnants. All exports preserved via direct path in main barrel.
</objective>

<context>
- `modules/prompts/todd.md` — old v0.1 TDD enforcement prompt (23 lines), superseded by `modules/prompts/tdd/pre-build.md`, `post-task.md`, `post-build.md`
- `modules/prompts/walt.md` — old v0.1 quality check prompt (18 lines), superseded by `modules/prompts/quality/pre-build.md`, `post-build.md`, `post-unify.md`
- `packages/coda/src/modules/index.ts` — barrel re-exporting `createModuleSystem`, `buildHookContext`, `getModulePromptForHook`, `loadModuleConfig` from `../workflow/module-integration`
- `packages/coda/src/modules/__tests__/modules.test.ts` — 3 tests verifying the barrel re-exports work
- `packages/coda/src/index.ts` line 19: `export * from './modules'` — needs to point directly to `./workflow/module-integration`
- Registry prompt resolution uses `modules/prompts/{module}/{hook}.md` paths — the old flat files are never loaded by the registry
</context>

<acceptance_criteria>
- **AC-1:** `modules/prompts/todd.md` and `modules/prompts/walt.md` no longer exist
- **AC-2:** `packages/coda/src/modules/` directory no longer exists (barrel + test removed)
- **AC-3:** `packages/coda/src/index.ts` exports `createModuleSystem`, `buildHookContext`, `getModulePromptForHook`, `loadModuleConfig` directly from `./workflow/module-integration`
- **AC-4:** All 433 existing tests pass (no regressions)
- **AC-5:** `tsc --noEmit` clean for both packages
- **AC-6:** New subdirectory prompt files (`modules/prompts/tdd/`, `modules/prompts/security/`, `modules/prompts/architecture/`, `modules/prompts/quality/`, `modules/prompts/knowledge/`) are untouched
</acceptance_criteria>

<tasks>
<task id="1" title="Delete old flat prompt files">
<files>
- modules/prompts/todd.md (DELETE)
- modules/prompts/walt.md (DELETE)
</files>
<action>
Delete `modules/prompts/todd.md` and `modules/prompts/walt.md`. These are the old v0.1 flat prompt files superseded by the subdirectory structure.
</action>
<verify>
Run: `ls modules/prompts/todd.md modules/prompts/walt.md 2>&1` — should show "No such file"
Run: `ls modules/prompts/` — should show only directories (architecture, knowledge, quality, security, tdd)
</verify>
<done>AC-1, AC-6 — old flat files gone, new subdirectory prompts untouched</done>
</task>

<task id="2" title="Remove legacy modules barrel and update main index">
<files>
- packages/coda/src/index.ts (MODIFY — update re-export path)
- packages/coda/src/modules/index.ts (DELETE)
- packages/coda/src/modules/__tests__/modules.test.ts (DELETE)
</files>
<action>
1. In `packages/coda/src/index.ts`, replace `export * from './modules'` with:
   ```typescript
   export {
     createModuleSystem,
     buildHookContext,
     getModulePromptForHook,
     loadModuleConfig,
   } from './workflow/module-integration';
   ```
2. Delete `packages/coda/src/modules/__tests__/modules.test.ts`
3. Delete `packages/coda/src/modules/index.ts`
4. Remove the empty `packages/coda/src/modules/` directory (including `__tests__/`)
</action>
<verify>
Run: `ls packages/coda/src/modules/ 2>&1` — should show "No such file or directory"
Run: `cd packages/core && bun test` — 179 pass, 0 fail
Run: `cd packages/coda && bun test` — should pass with count reduced by 3 (barrel re-export tests removed)
Run: `cd packages/core && npx tsc --noEmit` — clean
Run: `cd packages/coda && npx tsc --noEmit` — clean
</verify>
<done>AC-2, AC-3, AC-4, AC-5 — barrel removed, exports preserved, all tests pass, tsc clean</done>
</task>
</tasks>

<boundaries>
**DO NOT CHANGE:**
- Any file under `modules/prompts/tdd/`, `modules/prompts/security/`, `modules/prompts/architecture/`, `modules/prompts/quality/`, `modules/prompts/knowledge/`
- `packages/core/src/modules/` (registry, dispatcher, types — these are the real module system)
- `packages/coda/src/workflow/module-integration.ts` (the actual implementation being re-exported)
- Any other exports in `packages/coda/src/index.ts`

**SCOPE LIMITS:**
- This is deletion + re-pointing only. No new features, no refactoring.
- Test count will decrease by 3 (the removed barrel tests). This is expected, not a regression.
</boundaries>

<verification>
1. `modules/prompts/` contains only directories: tdd, security, architecture, quality, knowledge
2. `packages/coda/src/modules/` directory does not exist
3. `packages/coda/src/index.ts` exports module symbols from `./workflow/module-integration`
4. `bun test` passes in both core and coda packages
5. `tsc --noEmit` clean in both packages
</verification>

<success_criteria>
- [ ] Old flat prompt files deleted (AC-1)
- [ ] Legacy modules barrel directory removed (AC-2)
- [ ] Main index re-export updated (AC-3)
- [ ] Tests pass (AC-4) — expected count: 179 core + 251 coda = 430
- [ ] TypeScript clean (AC-5)
- [ ] New prompt files untouched (AC-6)
</success_criteria>

<module_dispatch>
Pre-plan advisory: DEAN(50) skip | ARCH(75) 0 concerns | SETH(80) skip | TODD(100) tests exist ✓ | IRIS(150) 0 anti-patterns | DOCS(200) skip | RUBY(250) skip
Pre-plan enforcement: no blocks
Post-plan: TODD(100) — plan type is `execute`, not `tdd`. TDD candidates exist but changes are deletions only — no testable behavior to add. No type change suggested.
</module_dispatch>

<output>
SUMMARY.md at `.paul/phases/32-module-cleanup/32-01-SUMMARY.md`
</output>
