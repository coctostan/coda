---
phase: 57-e2e-re-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md
autonomous: true
---

<objective>
## Goal
Re-run Script A (greenfield URL shortener → analytics) against the shipped v0.10 code and produce a findings report that proves, with direct evidence, whether CODA now compounds in practice.

## Purpose
Phases 53–56 landed the specific fixes for the v0.8 live-test failures: tool-only lifecycle entry (`coda_forge`, `coda_focus`), evidence-backed UNIFY artifacts, runtime-portable `coda_run_tests` plus deeper write-gate hardening, and stronger lifecycle-first prompt guidance. Phase 57 is the milestone close. It does not get to claim success from unit tests alone; it must show a real inner agent can walk Script A end-to-end without slash-command crutches or manual `.coda/state.json` edits, produce non-boilerplate overlays/reference docs, and measurably carry Issue 1 knowledge into Issue 2.

## Output
- `docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md` — the authoritative live re-validation report, written with a binary verdict: validated or still broken.
- A fresh disposable external test workspace (recommended path: `~/pi/workspace/coda-test-shortener-v010`) preserving the generated `.coda/`, source, tests, and git history for inspection.
- Recorded evidence for each milestone success bullet: lifecycle entry, TDD gate behavior, Issue 1 UNIFY artifacts, Issue 2 carry-forward, functional verification, and any residual failure mode.
- `.paul/phases/57-e2e-re-validation/57-01-SUMMARY.md` at UNIFY reconciling the live run, findings report, and milestone decision.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/milestones/v0.10.0-ROADMAP.md
.paul/PRD.md
/Users/maxwellnewman/pi/workspace/thinkingSpace/explorations/cmux-v08-test-scripts.md

docs/v0.8/E2E-COMPOUNDING-FINDINGS.md

## Prior Work
.paul/phases/52-greenfield-compounding-test/52-01-SUMMARY.md — the baseline failure report. Phase 57 must explicitly compare against its F1–F8 findings and avoid giving CODA credit for improvements that only come from in-conversation memory.

.paul/phases/53-agent-entry-points/53-01-SUMMARY.md — Phase 53 shipped `coda_forge`, `coda_focus`, and the `bare-workspace-lifecycle.test.ts` proof that tool-only lifecycle entry works in-process. The live run must now prove the inner agent naturally uses that surface.

.paul/phases/54-unify-produces-artifacts/54-01-SUMMARY.md — Phase 54 made `unify→done` evidence-based. The live run must show real overlays and reference-doc updates on disk, not just passing integration fixtures.

.paul/phases/55-supporting-systems-repair/55-01-SUMMARY.md — Phase 55 fixed `coda_run_tests`, deeper `.coda/` write-gate bypasses, and config migration. Script A must exercise these fixes without shell-only or manual-state workarounds.

.paul/phases/56-lifecycle-first-prompts/56-01-SUMMARY.md — Phase 56 strengthened bootstrap/phase/build/next_action guidance. The live run must show whether that guidance is strong enough to keep the agent inside CODA without repeated nudges.

## Source Files
packages/coda/src/tools/__tests__/bare-workspace-lifecycle.test.ts          # proof that forge → create → focus works without slash commands
packages/coda/src/tools/coda-forge.ts                                       # live FORGE handoff wording
packages/coda/src/tools/coda-focus.ts                                       # live focus tool wrapper
packages/coda/src/workflow/issue-activation.ts                              # focusIssue() phase/branch behavior
packages/coda/src/tools/coda-status.ts                                      # next_action guidance the inner agent sees
packages/coda/src/pi/hooks.ts                                               # before_agent_start bootstrap + write-gate perimeter
packages/coda/src/workflow/phase-runner.ts                                  # focused phase guidance
packages/coda/src/workflow/build-loop.ts                                    # BUILD/TDD protocol guidance
packages/coda/src/tools/coda-run-tests.ts                                   # live TDD gate execution path
packages/coda/src/tools/__tests__/coda-run-tests.test.ts                    # runtime-portability expectations
packages/coda/src/workflow/__tests__/unify-artifacts-e2e.test.ts            # evidence-backed UNIFY expectation
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
modules.yaml loaded from `/Users/maxwellnewman/.pi/agent/skills/pals/modules.yaml` (kernel_version 2.0.0)

**Advisory:**
- `[dispatch] pre-plan advisory: ARCH(75) → arch_context (pattern=workspace monorepo with strict layered architecture; Phase 57 is validation-only and keeps repo changes scoped to one new findings doc plus an external disposable test workspace; no package boundary changes planned) | SETH(80) → sec_warnings=0 in repo scope; treat any manual .coda/state.json edit or slash-command fallback during Script A as evidence of failure, not as an allowed workaround | TODD(100) → Bun test infra detected (`package.json` uses "bun test"; 55 in-repo test files found); tdd_candidates={bare-workspace lifecycle entry, coda_run_tests runtime path, unify-artifacts evidence gate}; plan type remains execute because Phase 57 validates shipped behavior rather than implementing repo code, but the live run must explicitly verify `coda_run_tests` unlocks TDD in practice | IRIS(150) → review_flags=0 in planned repo file scope | DAVE(200) → no CI config discovered in repo root scope; CI is out of scope for this live validation | DOCS(200) → doc_warnings={create `docs/v0.10/` findings report during APPLY; existing README drift remains tracked in STATE.md and stays out of scope here} | OMAR(205) → observability scope limited; preserve current DEBUG=coda:* behavior and log any missing runtime diagnostics in findings instead of patching code mid-run | CODI(220) → skipped — no extractable repo-confirmed symbols in the phase detail beyond validation artifacts/tests | RUBY(250) → debt_flags={validation-only phase; no repo production-file growth allowed}`
- `[dispatch] pre-plan advisory: skipped ARIA, LUKE, DANA, GABE, PETE, REED, VERA (no UI/data/API/performance/resilience/privacy repo changes in scope)`

**Enforcement:**
- `[dispatch] pre-plan enforcement: DEAN(50) → dep_warnings={critical:1, high:3, moderate:0, low:0}; ".paul/dean-baseline.json" present (2026-04-17T00:54:41Z); current bun audit matches the acknowledged baseline exactly (protobufjs critical + 3 basic-ftp highs), delta=0. PASS`

No blockers. Planning posture used: `medium` collaboration (project default) with `direct-requirements` framing because the milestone success criteria are already explicit.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)
No SPECIAL-FLOWS.md configured for this project — skills section informational only.
</skills>

<acceptance_criteria>
## AC-1: Script A enters and stays in the CODA lifecycle without manual state hacks
```gherkin
Given a fresh external workspace and Pi+CODA loaded with the shipped v0.10 extension
When the outer agent follows Script A naturally through FORGE, Issue 1 SPECIFY, and Issue 1 PLAN
Then the inner agent reaches a focused issue and an executable plan without any manual `.coda/state.json` edits
And no slash commands are needed after startup to move through the lifecycle
And any nudges stay within Script A's "hint, not instruction" rule and are recorded in the findings report
```

## AC-2: Issue 1 proves the Phase 53–55 fixes in a live run
```gherkin
Given Issue 1 progresses through BUILD, VERIFY, and UNIFY in the fresh workspace
When the inner agent completes the URL-shortener issue
Then `coda_run_tests` succeeds as the live TDD-gate path or the exact failure is captured as a finding
And UNIFY produces at least one non-empty overlay under `.coda/modules/*.local.md`
And UNIFY updates `.coda/reference/ref-system*.md` with the actual URL-shortener capabilities
And the issue advances to done without manual `.coda/` edits or hidden bypasses
```

## AC-3: Issue 2 shows measurable carry-forward or explicitly proves the remaining gap
```gherkin
Given Issue 1 artifacts exist on disk after UNIFY
When the outer agent requests the analytics feature as Issue 2 and observes the second lifecycle run
Then the findings report records whether Issue 2 reused Issue 1 patterns, overlay guidance, or reference-doc knowledge
And the comparison includes concrete evidence such as fewer mistakes, fewer nudges, reuse of file/test structure, or explicit pattern references
And if carry-forward is absent, the report names the remaining failure precisely instead of crediting conversation memory as CODA compounding
```

## AC-4: The findings report is milestone-decision ready
```gherkin
Given Script A execution completes or reaches a meaningful failure point
When the report is written to `docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md`
Then it includes setup metadata, results summary, compounding evidence, prompt effectiveness, issues found, artifact inventory, and functional verification
And it explicitly compares the v0.10 run against the v0.8 findings baseline
And it ends with a binary verdict: `validated` or `still broken`, naming the specific residual failure mode if broken
```

## AC-5: Validation stays read-only with respect to shipped CODA code
```gherkin
Given the live re-validation plan is complete
When repo diffs and baseline tests are checked
Then `bun test` is green before phase close
And no product source files under `packages/` or prompt data under `modules/` are changed by this phase
And the repo-side implementation deliverable is limited to the new findings report plus normal PALS lifecycle artifacts
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1 — Establish the clean v0.10 baseline, fresh Script A workspace, and report scaffold</name>
  <files>docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md</files>
  <action>
    Create the report scaffold first so observations are written as the run happens, not reconstructed from memory later:
    - Create `docs/v0.10/` and seed `E2E-COMPOUNDING-FINDINGS-v2.md` with the same major sections as the v0.8 report, plus two explicit additions: `Comparison vs v0.8 Findings` and `Milestone Verdict`.
    - Record the exact inner-agent model, extension load method, repo commit under test, test workspace path, and any environment deviations up front.

    Re-establish the shipped baseline before starting the live run:
    - Run `bun test` in this repo and record the baseline counts in the report.
    - Run a targeted confidence suite covering the four v0.10 fixes: tool-only lifecycle entry, UNIFY evidence gate, `coda_run_tests`, and lifecycle-first prompts. At minimum include `bare-workspace-lifecycle.test.ts`, `unify-artifacts-e2e.test.ts`, `coda-run-tests.test.ts`, and the most relevant prompt/next_action suites.
    - If the baseline test suite is red, stop the live run and record that Phase 57 is blocked by a regression.

    Prepare the disposable external workspace for Script A:
    - Use a fresh path (recommended: `~/pi/workspace/coda-test-shortener-v010`) so the v0.8 artifacts remain inspectable.
    - Recreate the bare Bun/TypeScript skeleton from Script A: `package.json`, `tsconfig.json`, `src/`, `tests/`, and initial git commit.
    - Start CMUX + Pi with the local CODA extension under the same model family used in the v0.8 baseline if available; if the exact model is unavailable, stop long enough to record the replacement and why comparability changed.
    - Disable megapowers or any other prompt-injecting extension before testing.

    Execute FORGE naturally and verify the initial success criteria:
    - Send the natural onboarding request from Script A; answer clarifying questions naturally.
    - Verify externally that `.coda/` exists, `coda.json` is reasonable, and the first reference docs are project-specific rather than boilerplate.
    - Do not use slash commands or manual `.coda` edits to get unstuck. If an operator-only fallback becomes necessary, record the exact step as milestone-failure evidence before continuing only if it adds downstream diagnostic value.
    - Apply the original 3-nudge maximum from Script A. If the inner agent stays stuck after 3 hints, record the failure explicitly and move on rather than coaching it into success.
  </action>
  <verify>
    command -v cmux && command -v pi && command -v bun
    bun test
    bun test packages/coda/src/tools/__tests__/bare-workspace-lifecycle.test.ts packages/coda/src/workflow/__tests__/unify-artifacts-e2e.test.ts packages/coda/src/tools/__tests__/coda-run-tests.test.ts packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/workflow/__tests__/issue-activation.test.ts packages/coda/src/tools/__tests__/coda-forge.test.ts
    cmux read-screen coda
    test -f docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md
  </verify>
  <done>AC-1 and AC-5 are set up with a green repo baseline, a fresh external workspace, and a report scaffold ready to capture live evidence.</done>
</task>

<task type="auto">
  <name>Task 2 — Run Issue 1 through BUILD/VERIFY/UNIFY and verify real compounding artifacts</name>
  <files>docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md</files>
  <action>
    Drive Issue 1 (URL shortener) through the full lifecycle exactly as Script A intends:
    - Send the natural SPECIFY prompt for the URL shortener, confirm the issue and acceptance criteria are meaningful, then send the natural PLAN prompt.
    - Observe whether the inner agent stays in lifecycle work or reverts to build-first behavior. Record any moments where it reads CODA source to figure out the tool flow instead of following injected guidance.
    - After plan creation, continue naturally into BUILD and VERIFY; do not tell the agent which `coda_*` tools to call.

    Focus the evidence collection on the Phase 53–55 fixes:
    - Confirm lifecycle entry and task progression use the tool surface rather than slash commands or manual state edits.
    - Confirm the live test path for TDD uses `coda_run_tests` successfully enough to unlock/build, or capture the exact failure if the agent still falls back to raw `bun test` or gets stuck.
    - Record whether write-gate hardening ever needs to intervene or whether the agent avoids illegal `.coda` mutations naturally.

    Treat UNIFY as the critical observation window:
    - After Issue 1 reaches UNIFY, verify externally that `.coda/reference/ref-system*.md` was updated with concrete URL-shortener capabilities.
    - Verify at least one overlay exists under `.coda/modules/*.local.md` and inspect the content quality; reject boilerplate, empty headings, or generic advice as success.
    - Verify the completion record exists, includes `unify_review_status`, and is consistent with the artifacts present on disk.
    - If any part of Issue 1 fails, keep the evidence exact: what the agent did, what artifacts exist, what artifacts are missing, and which milestone bullet remains unsatisfied.
  </action>
  <verify>
    cd ~/pi/workspace/coda-test-shortener-v010 && bun test
    ls ~/pi/workspace/coda-test-shortener-v010/.coda/reference/
    ls ~/pi/workspace/coda-test-shortener-v010/.coda/modules/
    cat ~/pi/workspace/coda-test-shortener-v010/.coda/reference/ref-system*.md
    cat ~/pi/workspace/coda-test-shortener-v010/.coda/modules/*.local.md
    ls ~/pi/workspace/coda-test-shortener-v010/.coda/records/
  </verify>
  <done>AC-1 and AC-2 are satisfied or explicitly failed with direct on-disk evidence for lifecycle entry, TDD-gate behavior, ref-system updates, overlays, and completion records.</done>
</task>

<task type="auto">
  <name>Task 3 — Run Issue 2, perform functional verification, and write the binary milestone verdict</name>
  <files>docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md</files>
  <action>
    Run the analytics issue as the true compounding test:
    - Send the natural Issue 2 request from Script A and observe whether the inner agent benefits from Issue 1's artifacts without being told how.
    - Capture concrete evidence for or against carry-forward: fewer nudges, fewer mistakes, direct reuse of file/test patterns, explicit references to known capabilities, or visible reliance on overlay/ref-system guidance.
    - Do not count conversation memory alone as CODA compounding. If the agent improves only because of immediate session memory, call that out explicitly.

    Complete the live validation with end-to-end verification:
    - Run the final functional checks (shorten, redirect, stats, persistence) against the external project.
    - Re-check `.coda/modules/*.local.md` and `ref-system*.md` after Issue 2 to see whether the artifacts grew meaningfully.
    - Preserve the external workspace for inspection, then close the CMUX session cleanly.

    Finalize the report as a milestone-decision artifact:
    - Fill in the results table for both issues and every lifecycle phase.
    - Add a comparison section mapping Phase 57 outcomes back to v0.8 findings F1–F8.
    - End with an explicit verdict: either v0.10 validates the compounding hypothesis, or it still fails, naming the exact remaining break.
    - Keep the language evidence-first. If the run is mixed, say exactly which milestone bullets passed and which did not; do not move the goalposts.
  </action>
  <verify>
    curl checks against the running external app for POST /shorten, GET /:code redirect, and GET /stats/:code
    cat ~/pi/workspace/coda-test-shortener-v010/.coda/modules/*.local.md
    cat ~/pi/workspace/coda-test-shortener-v010/.coda/reference/ref-system*.md
    test -f docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md
    grep -n "Milestone Verdict\|validated\|still broken" docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md
    git diff --stat -- packages modules README.md docs/coda-spec-v7.md
  </verify>
  <done>AC-3, AC-4, and AC-5 are satisfied: the report captures the full Script A evidence set, functional verification, explicit comparison vs v0.8, and a binary milestone verdict without changing shipped CODA code.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- packages/** (Phase 57 is validation-only; shipped CODA code is under test, not under modification)
- modules/** prompt data and evals
- README.md and docs/coda-spec-v7.md (tracked follow-ups remain out of scope during this live validation run)
- Any `.coda/` directory inside this repo root
- The archived v0.8 findings report (`docs/v0.8/E2E-COMPOUNDING-FINDINGS.md`) — treat as read-only comparison baseline

## SCOPE LIMITS
- No bug fixes, prompt tweaks, or emergency patching mid-run; document failures instead
- No new dependencies
- No Script B / brownfield work in this phase
- No manual `.coda/state.json` edits, slash-command fallbacks, or hidden tool-surface bypasses after startup; if required, record them as failure evidence before deciding whether to continue for diagnosis
- Keep the external test workspace disposable and separate from repo sources
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `command -v cmux && command -v pi && command -v bun`
- [ ] `bun test`
- [ ] `bun test packages/coda/src/tools/__tests__/bare-workspace-lifecycle.test.ts packages/coda/src/workflow/__tests__/unify-artifacts-e2e.test.ts packages/coda/src/tools/__tests__/coda-run-tests.test.ts packages/coda/src/pi/__tests__/hooks.test.ts packages/coda/src/tools/__tests__/coda-status.test.ts packages/coda/src/workflow/__tests__/issue-activation.test.ts packages/coda/src/tools/__tests__/coda-forge.test.ts`
- [ ] `cd ~/pi/workspace/coda-test-shortener-v010 && bun test`
- [ ] Functional curl checks pass for shorten / redirect / stats
- [ ] `test -f docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md`
- [ ] `grep -n "Milestone Verdict\|validated\|still broken" docs/v0.10/E2E-COMPOUNDING-FINDINGS-v2.md`
- [ ] `git diff --stat -- packages modules README.md docs/coda-spec-v7.md` shows no product-source drift
- [ ] All acceptance criteria are satisfied or the report explicitly names which milestone bullets failed and why
</verification>

<success_criteria>
- All 3 tasks complete with evidence captured in real time, not reconstructed after the fact
- The run proves or falsifies each v0.10 milestone success bullet explicitly
- Issue 1 either produces real overlays + ref-system updates or the report names the exact reason it failed
- Issue 2 either shows measurable CODA-mediated carry-forward or the report proves the remaining compounding gap
- Repo baseline tests remain green and shipped CODA code stays unchanged during the phase
</success_criteria>

<output>
After completion, create `.paul/phases/57-e2e-re-validation/57-01-SUMMARY.md`
</output>
