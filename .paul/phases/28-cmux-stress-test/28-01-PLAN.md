---
phase: 28-cmux-stress-test
plan: 01
type: execute
wave: 1
depends_on: ["27-01"]
files_modified:
  - docs/v0.2.2/E2E-REAL-PROJECT-FINDINGS.md
autonomous: false
---

<objective>
## Goal
Execute the E2E Real Project Test script via CMUX — a fully autonomous Pi session builds a TODO CLI through the complete CODA lifecycle. Record every interaction as pass/fail and catalog all issues found.

## Purpose
This is the definitive stress test for CODA. A real LLM agent (gpt-5.4-mini) in a real Pi session uses CODA tools to build a real project. No mocks, no unit tests — live end-to-end validation. The goal is not pass/fail but observation: where does CODA leak, confuse the agent, or break?

## Output
- `docs/v0.2.2/E2E-REAL-PROJECT-FINDINGS.md` — full findings report
- `~/pi/workspace/coda-test-todo/` — the built project for inspection
- Gap catalog for future fix phases
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md

## Prior Work
Phase 26: 10 operational fixes (LIVE-01 through LIVE-10)
Phase 27: Deep code review — 10 additional findings fixed
386 tests passing, tsc clean

## Test Script
docs/v0.2.2/E2E-REAL-PROJECT-TEST.md — the authoritative test script

## Test Configuration
- Model: openai/gpt-5.4-mini, thinking high
- Extension: packages/coda/src/pi/index.ts
- Test project: ~/pi/workspace/coda-test-todo
- CMUX orchestration: send/read-screen pattern
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan enforcement: DEAN(50) → skip (no new deps)
[dispatch] pre-plan advisory: TODD(100) → 386 tests, no new test files planned (test is observational)
[dispatch] pre-plan advisory: IRIS(150) → skip
[dispatch] pre-plan advisory: DOCS(200) → skip
[dispatch] pre-plan advisory: RUBY(250) → skip
</module_dispatch>

<acceptance_criteria>

## AC-1: Test Infrastructure Ready
```gherkin
Given a clean workspace at ~/pi/workspace/coda-test-todo
When the setup script runs
Then the bare project exists with package.json, tsconfig.json, git init
And Pi launches in a CMUX pane with CODA extension loaded
```

## AC-2: Full Lifecycle Exercised
```gherkin
Given Pi+CODA running in the test pane
When the test script phases 1-7 are executed
Then forge, specify, plan, build, verify, and unify are all attempted
And each phase transition is recorded as pass/fail
```

## AC-3: Findings Report Complete
```gherkin
Given all phases have been attempted
When the findings report is written
Then it contains: results summary, checkpoint details, issues found, agent experience observations, and artifacts list
```

## AC-4: Functional Verification
```gherkin
Given the BUILD phase produced source files
When the built CLI is tested (add, list, done, remove)
Then each command's behavior is recorded as pass/fail
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Setup — workspace + CMUX pane + Pi launch</name>
  <files>(no source files — CMUX orchestration only)</files>
  <action>
    Execute the Setup section from docs/v0.2.2/E2E-REAL-PROJECT-TEST.md:

    1. Clean workspace: rm -rf ~/pi/workspace/coda-test-todo && mkdir -p ~/pi/workspace/coda-test-todo
    2. Create bare project: package.json, tsconfig.json, mkdir src tests, git init + commit
    3. Open CMUX pane: cmux new-split --name coda --size 60
    4. Launch Pi: cmux send coda "cd ~/pi/workspace/coda-test-todo && pi -e /path/to/index.ts --model 'openai/gpt-5.4-mini' --thinking high"
    5. Wait 15s, read-screen to verify Pi loaded

    Record SETUP-1 checkpoint.
  </action>
  <verify>
    cmux read-screen shows Pi loaded without errors.
  </verify>
  <done>AC-1 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CMUX pane with Pi+CODA running</what-built>
  <how-to-verify>
    1. Look at the CMUX pane — Pi should be running with gpt-5.4-mini
    2. CODA extension should be loaded (no errors in output)
  </how-to-verify>
  <resume-signal>Type "approved" to start the test phases</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute test phases 1-7</name>
  <files>docs/v0.2.2/E2E-REAL-PROJECT-FINDINGS.md</files>
  <action>
    Execute phases 1-7 from docs/v0.2.2/E2E-REAL-PROJECT-TEST.md sequentially.
    Follow the script exactly — send each cmux command, wait, read-screen, record results.

    For each phase:
    - Send the commands as specified in the script
    - Wait the specified sleep times
    - Read screen after each step
    - Record each checkpoint as PASS/FAIL with evidence
    - If something fails, document the error and continue

    When CODA or the inner agent needs input (questions, approvals, guidance), provide it as specified in the script.

    Write findings to docs/v0.2.2/E2E-REAL-PROJECT-FINDINGS.md as you go — don't wait until the end.

    Phases:
    1. FORGE — scaffold + populate coda.json + ref docs
    2. SPECIFY — create issue with 5 ACs
    3. PLAN — create plan + 3 tasks, advance to build
    4. BUILD — agent-driven TDD for 3 tasks
    5. VERIFY — check ACs
    6. UNIFY — completion record + ref-system update
    7. FUNCTIONAL — test the built CLI
  </action>
  <verify>
    E2E-REAL-PROJECT-FINDINGS.md exists with results for all phases.
    ~/pi/workspace/coda-test-todo has .coda/ directory and source files.
  </verify>
  <done>AC-2, AC-3, AC-4 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Teardown + finalize findings</name>
  <files>docs/v0.2.2/E2E-REAL-PROJECT-FINDINGS.md</files>
  <action>
    1. Send /exit to the Pi session
    2. Close the CMUX pane
    3. Finalize the findings report — ensure all sections are complete
    4. Keep ~/pi/workspace/coda-test-todo for inspection
    5. Add summary counts and overall assessment
  </action>
  <verify>
    E2E-REAL-PROJECT-FINDINGS.md is complete with all sections.
    CMUX pane closed.
  </verify>
  <done>AC-3 satisfied (finalized)</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- packages/core/ — no modifications
- packages/coda/src/ — no modifications (this is a test phase, not a fix phase)
- modules/prompts/ — no changes

## SCOPE LIMITS
- Execute the test script as-is — don't fix CODA bugs during the test
- Document all issues found — fixes go in a future phase
- If the inner agent gets stuck, provide guidance per the script but document the gap
- The test project (todo-cli) is ephemeral — it doesn't need to be production quality

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Pi launched successfully in CMUX pane
- [ ] All 7 test phases attempted
- [ ] Findings report written with results for each checkpoint
- [ ] Functional verification attempted
- [ ] CMUX pane cleaned up
- [ ] ~/pi/workspace/coda-test-todo preserved for inspection
</verification>

<success_criteria>
- Full CODA lifecycle exercised in a live Pi session
- Every interaction documented with pass/fail
- Issues catalog ready for future fix phases
- Clear assessment of CODA's production readiness
</success_criteria>

<output>
After completion, create `.paul/phases/28-cmux-stress-test/28-01-SUMMARY.md`
</output>
