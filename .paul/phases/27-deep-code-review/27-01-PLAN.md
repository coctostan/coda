---
phase: 27-deep-code-review
plan: 01
type: execute
wave: 1
depends_on: ["26-01"]
files_modified: []
autonomous: false
---

<objective>
## Goal
Run a comprehensive production-readiness code review of the CODA runtime (packages/coda/src/), catalog all findings, then fix critical and high-severity issues. This catches anything the Phase 26 systematic sweep missed.

## Purpose
Phase 26 fixed 10 known issues identified by targeted search. A deep code review examines the full codebase holistically — looking for patterns, architectural issues, edge cases, and security concerns that targeted grep can't find. Fixing findings in the same phase ensures Phase 28 (CMUX stress test) starts from the cleanest possible baseline.

## Output
- Code review report with categorized findings
- All critical/high findings fixed
- Medium/low findings documented for future work
- Updated test suite covering fixes
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md

## Prior Work
Phase 26 fixed 10 LIVE issues. 381 tests passing. CODA runtime should now be operational but may have deeper issues.

## Review Scope
packages/coda/src/pi/ — Extension entry, commands, hooks, tools (280+340+34+171 lines)
packages/coda/src/tools/ — All coda_* tools + new path-validation, sort-utils, report-findings
packages/coda/src/workflow/ — Build loop, phase runner, review/verify runners, module integration, context builder
packages/coda/src/forge/ — Scaffold, greenfield, types
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan enforcement: DEAN(50) → skip (no new deps)
[dispatch] pre-plan advisory: TODD(100) → 381 tests, Bun framework
[dispatch] pre-plan advisory: IRIS(150) → 0 anti-patterns
[dispatch] pre-plan advisory: DOCS(200) → skip
[dispatch] pre-plan advisory: RUBY(250) → skip
</module_dispatch>

<acceptance_criteria>

## AC-1: Comprehensive Review Completed
```gherkin
Given the full packages/coda/src/ codebase
When a deep code review is performed
Then findings are categorized by severity (critical/high/medium/low)
And each finding has: file, line, description, recommendation
```

## AC-2: Critical/High Findings Fixed
```gherkin
Given the review findings report
When critical and high severity issues are identified
Then each is fixed with appropriate code changes
And tests are added where applicable
```

## AC-3: No Regressions
```gherkin
Given all fixes applied
When the full test suite runs
Then all existing tests pass and tsc --noEmit is clean
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Deep code review via REV subagent</name>
  <files>(no source files modified — review only)</files>
  <action>
    Dispatch the code-reviewer subagent to perform a production-readiness review of packages/coda/src/.

    Review dimensions:
    1. **Security** — beyond LIVE-05/06 fixes: input validation, auth patterns, data exposure
    2. **Error handling** — beyond LIVE-08 try/catch: error propagation, recovery paths, edge cases
    3. **State consistency** — race conditions, partial writes, corrupt state recovery
    4. **API contracts** — type safety, schema/implementation mismatches, missing validations
    5. **Performance** — unnecessary reads, unbounded operations, memory leaks
    6. **Code quality** — dead code, duplicated logic, unclear abstractions
    7. **Testing gaps** — untested paths, missing edge case coverage
    8. **Runtime behavior** — will the code actually work in a live Pi session?

    Produce a findings report organized by severity.
  </action>
  <verify>
    Review report produced with categorized findings.
  </verify>
  <done>AC-1 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code review findings report</what-built>
  <how-to-verify>
    1. Review the findings list
    2. Confirm which critical/high items to fix
    3. Decide if any medium items should be promoted or deferred
  </how-to-verify>
  <resume-signal>Type "approved" with any adjustments to proceed with fixes</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fix critical and high findings</name>
  <files>(determined by review findings)</files>
  <action>
    For each critical/high finding from the review:
    1. Implement the fix
    2. Add tests where the fix introduces new behavior
    3. Verify no regressions

    Document medium/low findings in the SUMMARY for future reference.
  </action>
  <verify>
    cd packages/coda && bun test
    cd packages/core && bun test
    cd packages/coda && bunx tsc --noEmit
    cd packages/core && bunx tsc --noEmit
  </verify>
  <done>AC-2, AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- packages/core/ — no modifications to core types or state engine
- modules/prompts/ — no prompt file changes
- .paul/ — no direct modifications

## SCOPE LIMITS
- Fix only critical/high findings — medium/low documented for future
- No new features — this is purely quality improvement
- No architectural changes — fix issues within existing structure

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Code review report produced with categorized findings
- [ ] All critical/high findings addressed
- [ ] Medium/low findings documented
- [ ] `cd packages/core && bun test` — all pass
- [ ] `cd packages/coda && bun test` — all pass
- [ ] `cd packages/core && bunx tsc --noEmit` — clean
- [ ] `cd packages/coda && bunx tsc --noEmit` — clean
</verification>

<success_criteria>
- Comprehensive code review covering 8 dimensions
- All critical/high findings fixed
- No regressions
- Clean TypeScript build
- CODA ready for Phase 28 CMUX stress test
</success_criteria>

<output>
After completion, create `.paul/phases/27-deep-code-review/27-01-SUMMARY.md`
</output>
